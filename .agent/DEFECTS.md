# Defects Log

> **Phase 15 Gate PASS Status (2026-08-09):** All 11 gates G1-G11 PASS. Phase 1-14 final hardening resolves the active defects listed below (Active table = empty per Phase 14 close). New defects discovered during Gate execution: **0** (no new regressions). Final active defect count: **0 open**.

## Active Defects

| ID | Title | Phase/Module | Severity | Priority | Status | Reported By | Reported | Notes |
|----|-------|--------------|----------|----------|--------|-------------|----------|-------|
| — | **(None open at Phase 15 close)** | — | — | — | **CLOSED** | — | — | No active defects; 0 regressions introduced during gate execution (11/11 PASS). |

## Resolved Defects

| ID | Title | Phase/Module | Severity | Resolved By | Resolution | Fixed Commit |
|----|-------|--------------|----------|-------------|------------|--------------|
| D-010 | Integration test Startup-Fatal crash: Connection string validator `unsafe pattern #6` → `Environment.Exit(1)` kills test host process | Phase 15 / G4 Integration / `LivestockManagerWebFactory` | **Critical** | Infra Team | Root cause = dual: (a) Program.cs reads `ASPNETCORE_ENVIRONMENT` with DEFAULT="Production" via process-level `Environment.GetEnvironmentVariable(...)` **BEFORE** WebApplicationFactory ConfigureTestServices runs, so UseSetting was too late; (b) original factory used `EnableDevSeed=true` via UseSetting, which is a truthy seed flag that matches pattern #6 when isProduction=true. **Fix:** Added static constructor `static LivestockManagerWebFactory()` that sets `ASPNETCORE_ENVIRONMENT=Testing`, all 4 seed flags (`SeedDemoData=0`, `EnableDevSeed=false`, `EnableE2ESeed=0`, `ENV_ENABLE_DEV_SEED=0`), and canonical connection string, ALL with `EnvironmentVariableTarget.Process`. Static ctor runs BEFORE type used so Program.cs early-config picks them up. Changed `UseSetting(ASPNETCORE_ENVIRONMENT=Staging)` to `builder.UseEnvironment("Testing")`; removed EnableDevSeed=true. | 4ea7b78 (Phase 1-14 commit before gates) |
| D-011 | E2E script first run: `artifacts/logs` + 10 sub-dirs not existing caused `Tee-Object` DirectoryNotFoundException when writing logs | Phase 15 / G6 E2E script setup | Medium | DevOps | Added `New-Item -ItemType Directory -Force` batch creation of: artifacts/logs, artifacts/e2e/{results,screenshots,traces,videos,logs}, artifacts/{pdf-samples,backups,production-publish,release,testresults} (11 dirs total) at E2E script startup, before any stream redirection. | ef1fac7 → 4ea7b78 range |
| D-012 | Gate G8 backup-database script: relative path `-BackupPath ./artifacts/backups` resolved by SQL Server as relative to `...\MSSQL\Backup\` (SQL Server CWD), not repo working directory → `Operating system error 3 (The system cannot find the path specified.)` Msg 3201 | Phase 15 / G8 backup script invocation | High | DevOps | Pass absolute path: `$abs = (Join-Path (Get-Location) "artifacts\backups"); backup-database.ps1 -BackupDirectory $abs`; script uses full OS path. Also note: parameter name is `-BackupDirectory` (not `-BackupPath`). | Invocation-level fix in GATE (script OK when called correctly). |
| D-013 | `scripts/Generate-PdfSamples.ps1` Program.cs throwaway project v1 compiled with 27 compile errors (CS0272 inaccessible BaseAuditableEntity.Id setter; CS0122 InvoiceItem / Payment / Receipt paramless constructors `inaccessible due to protection level`; CS0246 missing BaseAuditableEntity ref; CS7036 Customer constructor required arg mismatch; CS0117 PaymentMethod.Bank not defined) | Phase 15 / G7 PDF generation | High | Infra + Domain | Root cause: incorrect assumption about entity constructors (used paramless + Id-object-initializer instead of factory constructors). Fix: replaced Program.cs entirely using the **correct constructors from tests/PdfMultiPageTests.cs**: `new Company(name)` then object init properties; `new Customer(companyId, code, name)`; `new Invoice(companyId, customerId, invoiceDate, dueDate)` then object init; `new InvoiceItem(invoiceId, description, quantity, unitPrice)` then DiscountPercent/TaxPercent init + Reflection for DiscountAmount/TaxAmount setters; `invoice.UpdateTotalsFromItemsAndCharges()` to auto-compute totals/subtotal/grand/paidAmount; `new Payment(companyId, customerId, invoiceId, paymentDate, PaymentMethod.Cash, amount)`; `new Receipt(companyId, paymentId, customerId, receiptDate, amountReceived)`; PaymentMethod enum uses `Cash`, `BankTransfer`, `Cheque`, `Card`, `Other` (not `Bank`). | scripts/Generate-PdfSamples.ps1 (v2 corrected) |
| D-001 | EfSequenceGenerator ADO.NET transaction not assigned to DbCommand → InvalidOperationException "ExecuteScalar requires the command to have a transaction..." when command runs under ambient RepeatableRead transaction on SequenceCounters | S4 / Infrastructure / EfSequenceGenerator | Critical | Infra Team | Rewrote generator to open direct `DbConnection.BeginTransactionAsync(IsolationLevel.RepeatableRead)` and explicitly assign `dbCommand.Transaction = transaction` before `ExecuteScalarAsync`; guard with using/rollback on error. | (MVP v1 commit) |
| D-002 | SequenceCounter (singular) table-name mismatch vs pluralized `DbSet<SequenceCounter>` → CREATE/UPDATE targeted wrong table name, sequential ID increment failed | S4 / Infrastructure / EfSequenceGenerator | Critical | Infra Team | Renamed SQL identifiers inside the rowlock+UPDLOCK+HOLDLOCK UPDATE/INSERT statement from `SequenceCounter` to `SequenceCounters` to match EF pluralized table name. | (MVP v1 commit) |
| D-003 | AuditLog `Action` field always empty string → empty-Action guard in `CreateAuditLogsAsync` filtered out every audit, producing 0-row AuditLogs table for all prior saves | S2 / AppDbContext.CreateAuditLogsAsync | High | Infra Team | Moved `Action = Entry.State switch` assignment BEFORE the per-property `foreach (property in ...)` loop so Action is non-empty / populated BEFORE the guard checks whether property-level value pairs exist. | (MVP v1 commit) |
| D-004 | Dashboard RuntimeBinderException: `'decimal' does not contain a definition for 'HasValue'` when rendering recent-livestock table row | S8 / HomeController.Index + Views/Home/Index.cshtml | High | Web Team | Replaced `ViewData["RecentLivestock"]` anonymous-object projection (which inferred `CurrentWeight` non-nullable `decimal`) with strongly-typed `LivestockSummaryDto` (declares `decimal? CurrentWeight`); changed razor from `(IEnumerable<dynamic>)recentLivestock` to explicit `IEnumerable<LivestockSummaryDto>` with `@using LivestockManager.Application.DTOs.Livestock` + `.Any()` null-guarded loop. Invoice summary block fixed identically. | (MVP v1 commit) |
| D-005 | AuditLogs COUNT(*) = 0 even after D-003 Action-ordering fix | S2 / AppDbContext.CreateAuditLogsAsync | High | Infra Team | Replaced the guard condition `AuditLogs.Local.Any(a => a.Id == Guid.Empty)` (always false: EF auto-generates a non-empty Guid PK for entities marked `ValueGenerated.OnAdd` the moment `.Add()` is called) with `AuditLogs.Local.Count > 0` which correctly counts pending local inserts regardless of key-generation strategy. | (MVP v1 commit) |
| D-006 | LivestockTypeId enum = 0 default binding → `ArgumentOutOfRangeException: Actual value was 0. (Parameter 'type')` in EfSequenceGenerator.GetPrefix switch on POST /livestock/register | S4 / LivestockRegisterViewModel | High | Web Team | Added auto-property initializer `= LivestockType.Ah` to `LivestockTypeId` on `LivestockRegisterViewModel` so GET renders the Ah radio pre-selected; snapshot verified `states: [checked, readonly]` on first load, ASP.NET binder now receives valid value 1 (Ah) even if user submits untouched. | (MVP v1 commit) |
| D-007 | AuditLogs still persisted = 0 rows AFTER fixing D-005; real root: `base.SaveChangesAsync()` resets EntityState `Added → Unchanged` and `Deleted → Modified/Unchanged`, then the `CreateAuditLogsAsync` Action switch re-reads `Entry.State` → gets `Unchanged` → switch arm `_ => auditEntry.Action` (null) → `if (Action.IsNullOrWhiteSpace) continue` drops every entry. Also `HandleSoftDeletes()` sets `Deleted → Modified` AFTER PrepareAuditEntries so Delete events would be mislabeled "Update" if Action were set after HandleSoftDeletes. | S2 / AppDbContext.SaveChangesAsync pipeline ordering | Critical | Infra Team | Moved the `auditEntry.Action = auditEntry.Entry.State switch { Added => "Create", ... }` loop to **immediately after `PrepareAuditEntries()` and BEFORE `UpdateTimestamps()` / `HandleSoftDeletes()` / `base.SaveChangesAsync()`**. Then inside `CreateAuditLogsAsync`, guard the switch with `if (string.IsNullOrWhiteSpace(auditEntry.Action))` as a safe fallback, so the pre-captured state is never overwritten by stale post-Save `Entry.State`. | (MVP v1 commit) |
| D-008 | PowerShell variable name `$pid` is an automatic reserved constant → `Cannot overwrite variable PID because it is read-only or constant` when killing port-5100 listener processes | DevOps / Port-kill script (local session) | Medium | Infra Team | Renamed local process-ID variable from `$pid` to `$processId` throughout the netstat-based kill loop. | (MVP v1 commit) |
| D-009 | Chromium error `ERR_UNSAFE_PORT`: originally requested port 6000 is on Chromium's unsafe-port blocklist → browser refuses to connect to any service listening on 6000 locally | DevOps / ASPNETCORE_URLS (original user-requested port) | Medium | Infra Team | Switched Kestrel listen URL to `http://localhost:5100` and updated all documentation/STATE.md/resume instructions; this port is well outside Chromium's 1–1024 + restricted well-known-port blocklist and is not proxied/conflicted. | (MVP v1 commit) |

## Severity Definitions
- **Critical**: Blocks progress, data loss, crash
- **High**: Incorrect behavior, no workaround
- **Medium**: Incorrect behavior with workaround, UI issue
- **Low**: Cosmetic, typo, minor UX

---

## ROUND2 Audit Blocker Defects (D1..D5) — Injected at Round2 Phase A Start

Mapped 1:1 to blockers C / D / E / F / G. Each must be resolved + verified before Phase I commit.

| ID | Title | Blocker Phase | Severity | Priority | Status | Reported | Notes / Acceptance |
|----|-------|:-------------:|----------|----------|--------|----------|--------------------|
| **D1** | audit.zip tracked binary (315 MB) committed in repo index + worktree; violates repository hygiene policy; 3.txt §C mandates *.zip ignored; requires `git rm --cached` + .gitignore correction + xUnit hygiene guard. | **C** | **Critical** | **Critical** | **New → In Progress** | 2026-08-09 | Fix: git rm --cached audit.zip; delete worktree file; update .gitignore to remove !audit.zip negation and add all 19 patterns + font TTF/OFL negations; add RepositoryHygieneTests.R1 enforcing git ls-files has zero violations. |
| **D2** | Transitive HIGH vulnerabilities present in runtime graph (12 occurrences): System.Text.Json 7.0.3, Microsoft.Extensions.Caching.Memory 8.0.0, Microsoft.Build 17.8.3; source = Microsoft.VisualStudio.Web.CodeGeneration.Design 8.0.4 direct PackageReference in Web.csproj (NOT required for build/runtime; scaffolding-only). | **D** | **Critical** | **Critical** | **New → Pending** | 2026-08-09 | Fix: Remove Microsoft.VisualStudio.Web.CodeGeneration.Design entirely from Web.csproj; inspect and either remove (if unused) or pin-to-safe Microsoft.Extensions.Caching.Memory in Domain/Application; restore/build exit 0; `dotnet list package --vulnerable --include-transitive` → 0 Critical+High among packages that ship in publish output; verify publish deps.json and DLL list exclude Microsoft.Build / System.Text.Json 7.0.3 / CodeGeneration.* |
| **D3** | Invalid 169-byte placeholder assets posing as genuine Noto Sans fonts under src/LivestockManager.Infrastructure/Resources/Fonts/NotoSans-{Regular,Bold}.ttf; PDF FormattedPdfWriter currently loads them without structural validation. Could cause corrupted output / parser crash / Unicode bugs. Plus Unicode rendering would be mis-advertised. | **E** | **Critical** | **Critical** | **New → Pending** | 2026-08-09 | Fix: Delete both 169-byte fake TTF files (and empty placeholder OFL.txt if present); add TrueTypeFontValidator.cs C# class enforcing ≥ 50KB, signature 0x00010000, numTables 8..40, 9 required tables, bounds checks; wire FormattedPdfWriter.EnsureFontsLoaded to run validator and set _fontsAvailable=false + _fontsInvalidPlaceholderDetected=true if invalid (safe WinAnsi Helvetica fallback always); update THIRD_PARTY_NOTICES.md Noto section with CURRENT FONT STATUS=UNICODE_BLOCKED_EXTERNAL_WITH_SAFE_BASIC_FALLBACK; add 4 TrueTypeFontValidatorTests (placeholder fails, valid stream passes, wrong sig fails, head missing fails). |
| **D4** | scripts/backup-database.ps1 BackupDirectory parameter default ./artifacts/backups resolves relative to current shell working directory (pwd), not repository root. When invoked from another dir (e.g., D-012 symptom) SQL Server receives a relative path or wrong absolute path, Msg 3201. Also missing write-probe test before sqlcmd. | **F** | **High** | **High** | **New → Pending** | 2026-08-09 | Fix: Compute $RepoRoot = (Resolve-Path (Join-Path $PSScriptRoot ..)); BackupDirectory normalized via [IO.Path]::GetFullPath((Join-Path $RepoRoot $BackupDirectory)); New-Item -Force dir; write probe .write-probe.tmp; pass absolute .bak path to sqlcmd; all 6 test cases PASS (rel/abs/spaces valid with .bak>=1MB each; invalid-drive / missing-DB / system-DB all exit nonzero); restore one backup and verify AspNetRoles/AspNetUsers/Companies/Livestock exist via SELECT TOP 0. |
| **D5** | LivestockDocConsistencyTests failing on historical audit/*.md files containing quoted stale meanings (Ah/Su/Sa/Ad/Sd associated with Aves/Poultry/Swine). Current scanner has no narrow ignore mechanism so either: (bad-option-1) delete evidence or (bad-option-2) weaken/whitelist audit/ wholesale. Both unacceptable per audit-evidence and test-weakening rules. | **G** | **Critical** | **Critical** | **New → Pending** | 2026-08-09 | Fix: Define narrow HISTORICAL_BASELINE_QUOTE_START/END HTML-comment markers; walk flagged audit/*.md and wrap ONLY the specific stale-quote block lines (not entire files); update scanner to single-pass Regex strip (Singleline) of marker-enclosed regions BEFORE running existing adjacency for-loop; keep adjacency rules intact, keep audit/PHASE1_SOURCE_INVENTORY.md exclusion, keep third-party LICENSE exclusions, audit/ NOT excluded wholesale; add 5 new tests G-T1..G-T5; filter test exit 0, full unit suite exit 0. |

---

## Status Definitions
- **New**: Reported, not triaged
- **Triaged**: Prioritized, assigned
- **In Progress**: Fix being worked on
- **Resolved**: Fix applied, awaiting verification
- **Verified**: Tested and confirmed fixed
- **Rejected**: Not a defect, duplicate, or out of scope
