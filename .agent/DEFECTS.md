# .agent/DEFECTS.md — Reports Tab user review corrections

| # | Defect Title | Root Cause | Severity | Reproducible | Fix Status | Evidence / Link |
|---|---|---|---|---|---|---|
| DFX-001 | Active Livestock by Type Farm filter non-functional | `ActiveLivestock.cshtml L5` `ViewData["Farms"] as List<dynamic>` runtime-fails because real type from `IFarmService.ListAsync` = `IList<FarmSummaryDto>`. Result = null → fallback empty List → dropdown contains only "All Farms" default; farm route parameter still received by service so server side does filter, but user can never actually pick a farm and can't verify. | High | Yes, 100% | **FIXED** | Root cause analysis Sec 4 reproduced. Views/Reports/[ActiveLivestock.cshtml](file:///C:/Projects/livestock/src/LivestockManager.Web/Views/Reports/ActiveLivestock.cshtml) typed correctly. Service validation of farmId authorization added. All farms authorized dropdown populated. CSV receives same farmId. Mobile parity page created also uses same typed pattern. |
| DFX-002 | Sales by Period has no farm filter | Original ReportsController SalesByPeriod had no farmId parameter; model grouped farms but user could not pre-filter to one farm | Medium | Yes | **FIXED** | SalesByPeriod signature now includes `Guid? farmId`; validation; filter form renders Farm dropdown; export CSV preserves farmId route value. |
| DFX-003 | Sales by Period labels use hash symbol "# Sold" | Original table header `# Sold` (SalesByPeriod.cshtml L54) and "head sold" sub-header used hash symbol count meaning | Medium | Yes | **FIXED** | Global "Number Sold" terminology across desktop table header, summary card titles, mobile kpi labels, CSV column header "Number Sold". Sale SaleNumber hash prefix not touched. |
| DFX-004 | Livestock Profitability no date range filter | Original controller `LivestockProfitability(Guid? farmId, …)` — no From/To Date; service method called was basic `LivestockProfitabilityAsync` which returned ALL livestock every status; filtered by farm only | High | Yes | **FIXED** | Added DateTime? from, DateTime? to + AsUtcDayStart/End. Dispatch via `LivestockProfitabilityWithDatesAsync`. Both null → old all-status behavior; any → realized sold-period filter. |
| DFX-005 | Mobile pages only existed for PnL not for 3 other reports | Reports menu separate mobile design pattern (phase 15+ convention) had 1/4 reports mobile-optimized. Others would render desktop tables inside mobile which violated Sec 19 | Medium | Yes | **FIXED** | 3 new Mobile*.cshtml pages created matching exact controller actions. 100% shared backend services = zero logic duplication. |
| DFX-006 | Build error RZ1006 "else/for block missing closing }" after mobile RFC-4 | Razor MobileSalesByPeriod ambiguous `@if (useDetailsForFarms)` nested inside `@for` block — stray `@` transitions to markup context too early causing mismatched open/closes | Build blocker | Yes 1 round | **FIXED** | Remove `@` prefix from the two useDetailsForFarms if blocks. Resolves 5 RZ errors + 4 CS namespace errors. Build 0W/0E after fix |
| DFX-007 | Build warning CS8602 Dereference possibly null ReportService ThenInclude chains | New ReportService Include(l=>l.SaleItem).ThenInclude(si=>si.Sale).ThenInclude(s=>s.Farm) — nullable reference analysis flagged potential dereference null even when Include wiring | Low (warning only, not error) | Yes | **FIXED** | Use dammit operators per project convention `l.SaleItem!.Sale!.Farm!` (also LivestockProfitabilityAsync same block). Build now clean 0W/0E. |
| DFX-008 | 404 Mobile reports first smoke test despite build pass | Backend sub-agent claimed wrote ReportsController mobile actions; actual Grep on file showed only MobileProfitLoss. 3 actions never persisted to controller. | High (mobile routes broken) | Yes first smoke | **FIXED** | Manually appended three new controller GET actions matching views. Rebuilt server DLLs then smoke OK 302 auth redirect. |
| DFX-009 | Old server served stale DLLs on port 5100 | Previous session server was still running before mobile controller actions fix; browser 404 for new mobile routes even after rebuild because `dotnet run --no-build` not restarted process | Low (infra only) | Yes first smoke run | **FIXED** | Netstat locate PID, kill processes 11576 6180, restart `dotnet run --no-build -c Release --urls 5100` wait 18s → Login now 200, all mobile 302. |

## Pre-existing intentional items (NOT regressions this round)
† Unit test two failures `PostPurchase_CreatesLivestockIntakeIdempotently` and `PostPurchase_LivestockInitialWeightLinkedIfProvided` throw DomainException by design — Purchase Invoices cannot create livestock automatically; Stock Addition is the sole entry path. Documented Phase 15/16 unchanged. Severity: Intentional (auditor attention). Status: NOT FIXED not a defect.
