# .agent/DECISIONS.md — Reports Tab user review corrections

| # | Decision | Context | Choice | Rationale | Trace (spec section) |
|---|---|---|---|---|---|
| D1 | Root cause ActiveLivestock farm filter | Dropdown showed only All Farms, could not pick specific authorized farm | Razor `ViewData["Farms"] as List<dynamic>` — runtime type from `IFarmService.ListAsync` is `List<FarmSummaryDto>` strong typed → cast result null → empty list fallback | Wrong cast type, not a controller/service filter-ignore bug. Simplest fix vs. adding a second dynamic adapter layer. | Sec 4 Active Livestock farm filter investigation; Sec 5 root-cause list item "Farm dropdown using incorrect values" |
| D2 | FarmId unauthorized cross-company strategy | Spec says NotFound/validation error/empty authorized safe result | Empty safe result + nullify the farmId locally so downstream query defaults to company scope zero rows | Consistent with existing convention elsewhere (Livestock/Sales controllers company-scoped); avoids throwing 500 on manipulated query params; user still gets report UI empty state not crash | Sec 5 Validate that selected FarmId belongs to authenticated company; Sec 16 boundaries |
| D3 | Sales Number Sold counting method | Could use sale header count, sale rows, sale items livestock-linked | Count SaleItems per Sale/Farm where `si.LivestockId.HasValue` (distinct livestock). Non-livestock general invoice lines NOT counted. | Matches spec Sec 8 Number Sold definition Number Sold "Count distinct livestock sold through recognized sale items. Do not count general/non-livestock invoice lines." Reversed sales excluded via status filter before aggregation. | Sec 9 Sales calculations |
| D4 | Profitability date basis with/without dates | Both/all dates empty user expects "all livestock like original old report"; with dates expects "realized sold period only" | Dispatch: BOTH fromDate AND toDate null → call original `LivestockProfitabilityAsync` (all statuses, no exclusion). Any from/to set → call CompleteSold on DischargedSold, filter DischargeDate/sale realization date. Historical costs always kept regardless report period | Matches spec Sec 11 Sec 12 semantics; avoids removing pre-report-range acquisition costs (Sec 12 forbidden incorrect calculation list item 2) | Sec 11 FromDate/ToDate rules; Sec 12 date semantics |
| D5 | Sale costs / Net Proceeds labels on new reports | Spec warns against displaying false zero columns for nonexistent features | Display ALL 4 seller cost columns because Sale entity now has CommissionAmount/SellerTaxAmount/TransportationAmount/OtherCostAmount + TotalAdditionalSaleCosts + NetSaleProceeds fields ALL populated from prior Sales Tab review corrections. No fabrication; real data. | Pre-existing prior phase `AddSaleCostsReversalPriceSnapshots` migration already in this branch; columns are accurate and non-zero. Correct for this branch state. | Sec 8 "If seller-side cost fields not yet implemented…" branch check; Sec 17.7 prior phase |
| D6 | "# Sold" label replacement scope | Hash-symbol usage could be ambiguous in document numbers (SALE-xxx) | Replace ONLY in Reports/SalesByPeriod related table headers, cards, labels, CSV column names, mobile cards. Do NOT touch the Sale.SaleNumber display logic nor any non-count hash. | Matches spec Sec 7 "Do not replace hash symbols where they have another legitimate meaning, such as a document number label." | Sec 7 Terminology rules |
| D7 | Mobile pages strategy vs responsive single view | Spec explicitly requires "Preserve separate mobile pages. Do not duplicate reporting logic". | Create NEW dedicated Mobile* views (not CSS responsive only) and reuse the EXACT same IReportService backend methods (shared 100%). Controller actions call identical services; only Razor rendering differs. | Matches dual-view architecture established in prior phases (Sales/Stock Addition/Livestock separate mobile); no duplicated logic anywhere, separate routes + views/shared backend. | Sec 2 bullet Preserve separate; Sec 19 Separate Mobile reports |
| D8 | Profit & Loss handling strategy | Spec mandates zero logic/layout changes | ProfitLossAsync service method + ProfitLoss view + MobileProfitLoss view + ProfitLoss CSV rows COMPLETELY untouched. Even shared DTOs were not modified (report DTO separate sales profitability paths). Even if a shared helper existed for dates — avoid touching. Zero modifications. | User confirmed "Profit & Loss is good." Most risky regression if inadvertently modified. | Sec 1, Sec 15, Sec 22 completion rule #25 "Profit & Loss remains unchanged." |
| D9 | Index migration/performance strategy | Spec required inspect indexes before adding additive migration | Inspected: Livestock (CompanyId, FarmId, Status, LivestockType) + Sale (CompanyId, FarmId, Date, Status) + SaleItem (SaleId, LivestockId) already indexed from prior phases. No new indexes needed. Skipped creating AddReportsIndexes migration. | Avoids redundant indexes; EF already filters by Status/CompanyId/FarmId/Date on Sales/Livestock; current baseline report queries are fast for demo data sizes. | Sec 17 Query and performance requirements |
| D10 | Authorization for new mobile report actions | Operational vs Financial must match desktop equivalents. | MobileActiveLivestock → `CanViewOperationalData`; MobileSalesByPeriod/MobileLivestockProfitability → `CanViewFinancialData`; identical role matrix desktop↔mobile. | Consistent with Sec 16; prevents accidental elevation of mobile routes (mobile route names don't match controller default auth attribute alone). | Sec 16 Operational report access / Financial report access |
