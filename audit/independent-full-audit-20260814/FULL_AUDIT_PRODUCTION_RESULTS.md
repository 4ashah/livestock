# Production results

`dotnet publish` succeeded. Exclusion scan found `appsettings.Development.json` inside the publish output, so the production package gate is FAIL. No release ZIP/SHA-256 is approved.
