# FULL AUDIT DEFECT REGISTER — Livestock Manager (Section 24)

**Audit Section:** 24
**Status:** COMPLETE — All findings classified and registered.
**Classification Authority:** Independent Senior Auditor

---

## Severity Legend

| Severity | Meaning | Release-Blocking? |
|---|---|---|
| **CRITICAL** | Authentication bypass, cross-company data leak, financial corruption (duplicate IDs / wrong totals), missing invoice items, Production TestUser seeding activated, public protected-documents download, data loss risk, destructive-restore without confirm, duplicate financial IDs. | **ALWAYS YES** |
| **HIGH** | Authorization failure, incorrect payment balance, broken core workflow, applicable HIGH runtime CVE with reachable code path, failed backup or restore (cannot recover), transaction inconsistency, wrong profitability numbers, unusable core workflow desktop/mobile, production startup fatal failure. | **ALWAYS YES** |
| **MEDIUM** | Wrong report filter, major responsive layout issue, form validation bug accepting invalid financial input, documentation / workflow inconsistency, recoverable workflow (rollback possible without data loss). | CONDITIONAL: If workflow/financial or tenant scope = Release-Blocking YES; cosmetic UI-only = with conditions OK |
| **LOW** | Cosmetic UI glitch, minor terminology wording issue, UX label could be clearer, non-operational docs spelling. | **NO (not Release-Blocking)** |

---

## Defect Register Table

| Defect ID | Severity | Module | Title | Evidence (How Reproduced) | Reproduction Steps | Expected | Actual | Route / Endpoint | Files Involved | DB Impact? | Security Impact? | Financial Impact? | Desktop Impact? | Mobile Impact? | Release-Blocking? | Minimal Recommended Correction | Required Regression Test | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| FAUD-0001 | CRITICAL | CompanyService | Cross-tenant data leak: CompanyService GetDefaultAsync / ListAsync no company filter | Static code inspection of CompanyService methods | 1. Login as Company A user. 2. Call service method that calls GetDefaultAsync. 3. Inspect returned Company entity. | Returns only Company A's company record. | Returns arbitrary first company across all tenants via unfiltered FirstOrDefault / ToList. | N/A (service-level; leaks to Dashboard/Settings pages) | `CompanyService.cs` GetDefaultAsync L~28-32; ListAsync L~40 | No (no writes) | **YES — CRITICAL: Cross-tenant read** | Indirect (financials routed through wrong company) | Yes (Dashboard) | Yes (MobileDashboard) | **YES** | Add `.Where(c => c.Id == companyId)` / `companyId` parameter where missing in both methods. | Unit test: 2 companies seeded; call GetDefaultAsync per companyId; Assert.Equal only correct Id returned. Service integration: 2 HTTP calls each user → each sees own company. | OPEN |
| FAUD-0002 | CRITICAL | HomeController | Financial data leak to Viewer/DataEntry via Dashboard CanViewOperationalData gate exposing RevenueMTD/PurchasesMTD/ExpensesMTD/NetOperatingResult/OutstandingInvoicesTotal/OverdueInvoicesTotal | Static analysis: Dashboard action uses [Authorize(CanViewOperationalData)] then reads financial aggregates. Program.cs policy defines Viewer + DataEntry as having CanViewOperationalData but EXPLICITLY LACKING CanViewFinancialData. | 1. Login as Viewer role. 2. Navigate to /Home/Index or /Home/MobileDashboard. 3. Inspect revenue, purchase, expense KPI cards. | Viewer/DataEntry see NO financial KPIs (only operational livestock counts). / OR route requires CanViewFinancialData. | Viewer/DataEntry view 7 financial aggregate values including MTD Revenue, MTD Expenses, Outstanding Overdue Invoices Totals GrandTotal Paid Outstanding. | `/Home/Index` `/Home/MobileDashboard` | `HomeController.cs` Dashboard lines ~40-80; MobileDashboard ~90-130. Dashboard view KPI cshtml sections. | No | **YES — financial data to non-authorized roles** | **YES — financial totals exposure** | Yes Dashboard cards | Yes MobileDashboard cards | **YES** | Either (a) move financial KPI sections into a separate partial gated by CanViewFinancialData policy check AND exclude Viewer/DataEntry from financial DTO fields; OR (b) change Dashboard/MobileDashboard to require CanViewFinancialData (breaks Viewer entry). | Integration test: Viewer + DataEntry HTTP GET /Home/Index → Assert that DTO fields RevenueMTD/PurchasesMTD/ExpensesMTD/NetOperatingResult/OutstandingInvoicesTotal/OverdueInvoicesTotal are 0 or excluded; CompanyAdmin GET same values non-zero. | OPEN |
| FAUD-0003 | CRITICAL | LivestockService | Tenant-resolution failure writes livestock into ARBITRARY first company cross-tenant. RegisterAsync lines 43-47 fallback `_db.Companies.FirstAsync(ct)` no filter. | Static code analysis path: controller passes companyId to service. If somehow empty (trust fallback pattern line 43 `if (companyId == Guid.Empty) companyId = dto.CompanyId;` then line 47 fallback to `FirstAsync` with NO WHERE). | 1. Construct POST /Livestock/Register with tampered hidden CompanyId = 00000000-0000-0000-0000-000000000000 (empty). 2. Controller passes empty → service trust fallback → FirstAsync arbitrary company. | Livestock created ONLY under logged-in user's own company. Cross-company write rejected if companyId mismatch. | Livestock record silently created under whichever company is alphabetically-first in Companies table across ALL tenants — cross-tenant WRITE. | `/Livestock/Register` POST `/Livestock/MobileRegister` POST `/StockAddition/Purchase` POST (flow through to RegisterAsync) | `LivestockService.cs` RegisterAsync L43-47; similar fallback lines in 6 other services (see FAUD-0007). | **YES — cross-tenant write into wrong company** | **YES — CRITICAL: Cross-tenant write + read** | **YES — financials posted to wrong company** | Yes | Yes | **YES** | Remove unfiltered FirstAsync fallback entirely. If companyId empty → throw DomainException("Invalid company scope"). All 6 trust-fallback sites must treat empty Guid as invalid identity, not accept dto fallback blindly. | Unit test: pass companyId=Empty to RegisterAsync → Assert.Throws<DomainException>; integration: POST Livestock with empty CompanyId form input → 400/redirect with error, no DB row created. | OPEN |
| FAUD-0004 | CRITICAL | PurchaseService | Purchase draft creation breaks transactional integrity: CreateDraftAsync performs 3 sequential SaveChangesAsync NO BEGIN TRANSACTION wrapper. Partial purchase header/items committed if mid-process fails. | Static inspection of transaction placement. 12 other service methods correctly use BeginTransaction; this method has NONE. | 1. Start CreateDraftAsync POST Purchases/Create. 2. Header inserted (SaveChanges 1 success, id returned). 3. Add first PurchaseItem SaveChanges 2 success. 4. Before update totals, force DB write failure (e.g., trigger decimal overflow). | All writes roll back atomically. No partial purchase visible in purchases index. | Header + items rows COMMITTED. Totals are stale or 0. Purchases index page shows invalid partial draft with mismatched subtotal. Users can open Draft Purchase with 0 totals against real database financial state. | `/Purchases/Create` POST `/Purchases/MobileCreate` POST | `PurchaseService.cs` CreateDraftAsync method body; compare with PostPurchaseAsync which correctly has `IDbContextTransaction` | **YES — corrupt DB rows** | No authorization bypass | **YES — financial totals corruption** | Yes draft visible in Index grid + Details | Yes Mobile Index page visible | **YES** | Wrap entire CreateDraftAsync in `await using var tx = await _db.Database.BeginTransactionAsync(ct);` + `await tx.CommitAsync(ct)` after final SaveChanges. Align with PostPurchase transaction pattern which is correct. | Unit/integration test: inject failure mid-CreateDraft via Strategy or throw; Assert post-failure SELECT COUNT FROM Purchases WHERE id = @draftId = 0; no orphan rows. Normal path: Assert 3 write total, 1 committed transaction. | OPEN |
| FAUD-0005 | CRITICAL | SaleService | Sale draft creation transactionless: CreateDraftAsync performs 3 sequential SaveChanges NO TRANSACTION wrapper mirroring FAUD-0004. | Static comparison with ConfirmSaleAsync which correctly wraps in transaction. CreateDraft has no using var tx = ... | 1. POST /Sales/Create → sale header saved SaveChanges 1 OK. 2. Add item(s) SaveChanges 2 OK. 3. Before RecalculateSaleTotals — trigger write error. | All 3 roll back atomically. No partial draft. | Header + SaleItems COMMITTED with stale GrandTotal=0 / Subtotal=0. Users can confirm zero-total draft sale later against zero totals producing wrong sale invoice/payment chain with missing financials. | `/Sales/Create` POST `/Sales/MobileCreate` POST | `SaleService.cs` CreateDraftAsync method. | **YES — corrupt sale rows** | No auth bypass | **YES — incorrect financial totals flowing downstream into invoices** | Yes | Yes | **YES** | Wrap entire CreateDraftAsync in BeginTransaction + Commit pattern exactly as ConfirmSaleAsync uses. | Same test pattern as FAUD-0004 but for SaleService. Assert no orphan sale rows on mid-process failure. | OPEN |
| FAUD-0006 | CRITICAL | Startup (Program.cs) | Production startup silently swallows migration failure: empty `catch { }` wrapping `await db.Database.MigrateAsync()` at Program.cs line 248-250. Application continues serving with ZERO schema applied, leads to SqlException from every page hitting DB / no table exists → effectively data-loss grade error in production if migration fails. | Static text pattern search for `catch { }` → 8 empty catches, top 2 are startup critical. | 1. Deploy application to new database. 2. SQL Server access exists but CREATE TABLE permission denied for migration principal (common least-privilege deploy pattern). 3. Startup runs. | Startup CRASHES (process exit non-zero) with clear error logged → deploy pipeline fails deployment → rollback to prior package. Migration failure MUST fail startup. | Migration exception SILENTLY CAUGHT. Startup appears "successful" to IIS / process monitor. Every subsequent request hits missing table → infinite 500 cascade without log entries. Deploy pipeline erroneously marks deploy succeeded. | N/A (Startup-time) Global impact to all routes. | `Program.cs` L248-250 MigrateAsync empty catch. Second critical empty catch L260-262 SeedAsync. | **POTENTIAL: if migration partially applied then user-requests write data, schema version mismatch = corruption.** | No | Yes (every financial action returns 500) | Yes | Yes | **YES** | Replace both empty catch blocks at L248-250 and L260-262 with `catch (Exception ex) { Log.Fatal(ex, "Database migration or seed failed during startup. Terminating."); Environment.Exit(1); throw; }` — fail-fast. | Integration test: deny permission to migration account; attempt startup → Assert app fails fast / process exit code non-zero; logs contain fatal message. | OPEN |
| FAUD-0007 | HIGH | PurchaseService + SaleService | AddItemAsync/RemoveItemAsync each have 2 SaveChanges without transaction (multi-write no atomicity). Update totals after modifying lines. Partial write = item added but totals not recalculated. | Static transaction audit: 5 critical methods transactionless total. | 1. Open existing draft Purchase. 2. Click "Add Item" → POST Purchases/AddItem. 3. SaveChanges 1 (insert PurchaseItem) succeeds. 4. SaveChanges 2 (UpdatePurchaseTotals) fails constraint. | Both 2 writes roll back → totals unchanged + no item. | PurchaseItem row EXISTS in DB. Purchases.Subtotal/TaxTotal/GrandTotal NOT updated. Purchase page shows stale totals but item is in the list. User Posts this purchase → wrong financial amounts. | `/Purchases/AddItem` POST `/Purchases/RemoveItem` POST `/Sales/AddSaleItem` POST `/Sales/RemoveSaleItem` POST (equivalent methods if present) | `PurchaseService.cs` AddItemAsync, RemoveItemAsync. | **YES — partial update** | No | **YES — wrong totals applied on Post** | Yes | Yes (mobile purchases uses same service) | **YES** | Wrap each AddItem/RemoveItem in transaction scope exactly as other services (PostPurchase pattern). | Test inject failure second Save; Assert item rolled back, totals unchanged. | OPEN |
| FAUD-0008 | HIGH | Cross-service (Sale/Invoice/Payment/Receipt) | 6 locations load Customer entity via FirstOrDefault (or equivalent) WITHOUT applying company filter after FK join — customer data cross-company load risk by navigating object graph. Customer belongs to Company A; but if InvoiceId from Company B URL-tampered passed → GetById loads invoice then accesses .Customer → no-where Customer yields A's customer data displayed in B context. | Static code analysis of all services using `.Customer` navigation on loaded entity without explicit CompanyId WHERE. 6 call sites confirmed. | 1. Login as Company B Accounts. 2. Tamper URL to Invoice/Details?id={CompanyAInvoiceId}. 3. Service GetById(id, companyId) applies company filter for Invoice but loads Invoice.Customer via lazy/no-filter. 4. Render Details. Check PDF generated - does it contain Company A customer name/address? | Customer entity MUST be re-verified: `if (invoice.Customer.CompanyId != companyId) throw`. Every Customer navigation dereference requires explicit company check. | Invoice passes company check (filtered). But `.Customer` object graph yields Company A customer. Customer details (Name, Phone, TaxId) rendered on Details page + PDF = cross-company customer PII leak. | `/Invoices/Details` GET `/Invoices/DownloadPdf` `/Sales/Details` GET `/Payments/Details` GET `/Receipts/DownloadPdf` `/Receipts/Details` GET | `SaleService.cs` ConfirmAsync + GetById; `InvoiceService.cs` GetById; `PaymentService.cs` GetById; `ReceiptService.cs` GetById + GetPdf. 6 total locations. | No (no write) | **YES — HIGH: Customer PII cross-tenant read via navigation property** | Financial: invoice PDF renders Company-A customer for Company-B invoice if IDs can be guessed. | Yes | Yes | **YES** | After any load with .Customer Include: add explicit check `if (entity.Customer != null && entity.Customer.CompanyId != companyId) return NotFound / throw new UnauthorizedAccessException`. Include(...) loads, manual CompanyId check is mandatory because no global CompanyId filter. | Integration test 2 companies 2 invoices each. Tamper id → Assert 404 / 403. Assert invoice PDF generation call for cross-company id throws / returns Unauthorized. | OPEN |
| FAUD-0009 | HIGH | Dependencies (NuGet) | HIGH severity transitive CVE-2024-43320 / GHSA-qj66-m88j-hmgj Microsoft.Extensions.Caching.Memory 8.0.0 reachable in Domain + Application production assemblies. dotnet list package --vulnerable confirms. | Section 3 clean worktree: dotnet list LivestockManager.sln package --vulnerable --include-transitive output. HIGH severity Microsoft.Extensions.Caching.Memory 8.0.0. transitive dependency in Domain/App. | 1. Production publish folder contains Microsoft.Extensions.Caching.Memory.dll version 8.0.0.42420. 2. Advisory GHSA-qj66-m88j-hmgj describes denial-of-service / data corruption on high memory pressure under specific usage patterns. | Version >= 8.0.5 where CVE fixed. Or package reference updated with explicit Version override. | 8.0.0 currently shipped. CVE path reachable because LivestockManager uses IMemoryCache (e.g., for settings, sequences, parent search lookups). | N/A (runtime DLL) | `LivestockManager.Domain.csproj` + `LivestockManager.Application.csproj` transitive closure via Microsoft.Extensions options/caching packages. | No | Security DoS / potential memory corruption = HIGH | Financial corruption if cache serves stale sequence / wrong totals during high memory | Yes (all endpoints) | Yes (all endpoints) | **YES** | Add explicit `<PackageReference Include="Microsoft.Extensions.Caching.Memory" Version="8.0.5" />` to both Web and Infrastructure projects (direct reference to override transitive). Then re-run `dotnet list package --vulnerable` → confirm no HIGH CRITICAL for production package. | Deploy artifact check: GAC the DLL version. Post-patch: dotnet list package vulnerable → 0 High/Critical for production graph. | OPEN |
| FAUD-0010 | HIGH | DocumentsController | Authorization mismatch: Upload action correctly requires CanViewFinancialData policy. But List + Download actions incorrectly gated CanViewOperationalData → Viewer/DataEntry roles can list and download financial PDF documents (invoices/receipts/statements) across the company. | Controller attribute inspection: DocumentsController lines 60-150. | 1. Login as Viewer role. 2. Navigate Documents/List?entityType=Invoice&entityId={}. 3. Click Download link for any invoice file that exists. | Viewer/DataEntry → 403 Forbidden because documents are financial records (invoices, receipts, purchase bills). Only CanManageAccounting / CanViewFinancialData may access. | HTTP 200 renders Document list page with download links. File bytes returned via Download 200 OK. Viewer role views invoice PDF content including Customer Tax ID, Totals, line items. | `/Documents/List` GET `/Documents/Download` GET | `DocumentsController.cs` L~38 List; L~152 Download; Upload correctly L~77 `[Authorize(CanViewFinancialData)]`. Upload correctly gated, List + Download incorrectly use base CanViewOperationalData controller-level policy. | No | **YES — Unauthorized document retrieval financial** | **YES — financial documents downloaded by non-authorized roles** | Yes | Yes (mobile page if links shared) | **YES** | Add explicit [Authorize(Policy = "CanViewFinancialData")] to List and Download action methods. Remove controller-level permissive. Or replace controller-level to match Upload CanViewFinancialData. | Integration test: 6 roles × Documents List/Download. Viewer/DataEntry → 403; Accounts/CompanyAdmin/SysAdmin → 200 OK. | OPEN |
| FAUD-0011 | MEDIUM | ProfitLoss Report | ProfitLoss report completely missing farmId parameter/filter. Action signature, service method, view all lack farm scope. Controller Reports/ProfitLoss passes only from/to dates; service IReportService.GetProfitLossAsync has no farmId parameter; returns company-wide aggregate only. User cannot filter by farm per farm-specific P&L. | Static inspection of report methods parameter lists. ActiveLivestock, SalesByPeriod, LivestockProfitability each have farmId. ProfitLoss is the ONLY report without. | 1. Open Reports/ProfitLoss?from=2026-01-01&to=2026-01-31. 2. Look for Farm filter dropdown. 3. Try append manually &farmId={knownFarmId}. | Farm dropdown available, farmId filter applied to service DTO, rows scoped per farm consistent with other reports. Manual &farmId= works. | No Farm filter UI rendered. Appending &farmId= in URL ignored (action doesn't accept → binding no-op). Company-wide data always shown even for FarmManager role whose authorizations expect farm-scoped views. | `/Reports/ProfitLoss` GET `/Reports/MobileProfitLoss` GET `/Reports/ProfitLoss?format=csv` CSV Export | `ReportsController.cs` ProfitLoss L152-189; MobileProfitLoss L193-200; `IReportService.cs` GetProfitLossAsync; `ReportService.cs` ProfitLoss method; `ProfitLoss.cshtml` view + CSV. | No | Tenant scope correctly Company but Farm scope missing = authorization gap for FarmManager | Financial: mixes Farm A + Farm B expense/sale/purchase rows in single P&L per farm operator | Yes (no filter) | Yes (mobile page no filter) | **YES** — financial reporting for farm scoped roles is broken | Add `Guid? farmId` parameter throughout controller → service → view filter → CSV. Mirror pattern exactly from SalesByPeriod or LivestockProfitability which have farm scoping. | Test: 2 farms with data. ProfitLoss farmId=FarmA only includes FarmA rows; total should be less than overall company total. CSV export same rows. FarmManager role sees dropdown with OWN farms only. | OPEN |
| FAUD-0012 | MEDIUM | ReportService (Profitability) | Complete Profit formula error + silent date degradation. (A) Complete = basicProfit − directExpenses. But FAUD SPEC mandates: Complete = NetSaleProceeds − TotalAcquisitionCost − ValidOperatingExpenses. Currently AdditionalAcquisitionCosts and AdditionalSaleCosts are displayed as informational columns NOT NETTED into Complete number. (B) If From/To dates are NULL (user opens first time no filter) → fallback variant called where Complete==Basic and DirectExpenses=0 with no warning. | Static read of ReportService CompleteLivestockProfitability and sibling LivestockProfitabilityWithDatesAsync vs no-dates variant line. | 1. Go to Reports/LivestockProfitability. 2. Do not select dates (default open). 3. Note Complete Profit ≈ Basic Profit values. 4. For animals with known Commission+Tax+Transport+OtherCost of non-zero amounts, note Complete does NOT subtract these. | Complete = (GrossSale − 4SellerCosts) − (PurchasePrice + Commission+Tax+Transport+Other) − DirectExpenses. No-date fallback should either show a warning banner "Default: unfiltered complete uses direct costs only" or apply the same correct formula across date-scoped and unscoped. | Complete under-stated by AdditionalSaleCosts and under-stated by AdditionalAcquisitionCosts. Net result: Complete is inflated (financial reporting wrong). | `/Reports/LivestockProfitability` GET CSV `/Reports/MobileLivestockProfitability` | `ReportService.cs` CompleteLivestockProfitability method; LivestockProfitabilityWithDatesAsync; no-dates fallback variant lines. | No | No | **YES — formula wrong** | Yes | Yes | **YES** — profitability totals off by acq cost components | Fix Complete formula NetSaleProceeds (GrossSale-AddlSaleCosts) - (Purchase+AddlAcqCosts) - DirectExpenses. Remove silent fallback so both with-dates and no-dates use identical formula. In Reports controller, if no dates provided still call WithDates variant with DateTime.MinValue / UtcNow (or warn). | Known-data test: 1 animal with Purchase=1000, AddlAcqCosts=200 (Commission 100+Tax50+Transport25+Other25), Sale=2000, AddlSaleCosts=150, DirectExpenses=100 → Assert Basic=1000, Complete=2000-150-1200-100 = 550. If current code returns 1000-100=900 for Complete, bug reproduced. | OPEN |
| FAUD-0013 | MEDIUM | PaymentService | Payment reversal audit trail loss: ReversedByUserId set to Guid.Empty instead of current user's ID. Line ~101 of ReverseAsync method = payment reversal audit missing who performed the reverse action. Violates financial audit requirements. | Static read of PaymentService ReverseAsync L~101. | 1. Create payment then reverse it. 2. Query Payments table ReversedByUserId column for reversed payment. 3. Check Audit log for the action. | ReversedByUserId = actual UserManager.GetUserId (logged-in user performing the reverse). | ReversedByUserId = 00000000-0000-0000-0000-000000000000 (empty). Cannot trace WHO reversed a payment in financial audit trail. | `/Payments/Reverse` POST (if endpoint) or via reversal workflow from Invoices | `PaymentService.cs` ReverseAsync line ~101. Also see ReceiptService.ReverseReceiptAsync L208 = DateTimeOffset.UtcNow (FAUD-0014). | Yes (important DB column left empty) | No (authorization correct, just who-performed missing) | Financial audit trail incomplete | Yes | Yes | **YES** | Accept reversedByUserId parameter in ReverseAsync service method; controller passes current userId. Fill ReversedByUserId = value (not Empty). Mirror pattern from Sale Reverse which correctly passes user. | Test: reverse a payment → SELECT ReversedByUserId == knownUserId; Guid.Empty → fail. | OPEN |
| FAUD-0014 | MEDIUM | ReceiptService | Receipt reversal uses DateTimeOffset.UtcNow hard-coded at line 208 instead of injected IDateTime.Now / UtcNow service. Breaks testability + clock accuracy during test time travel scenarios. | Static search for `DateTimeOffset.UtcNow` in infrastructure/ application code (should all be via IDateTime interface abstraction). | 1. Unit test class injects fake IDateTime to travel clock. 2. Call ReverseReceiptAsync. 3. Query ReversedAt column. | ReversedAt equals fake clock value set in mock. | ReversedAt equals actual system clock (ignores injected clock) because hard-coded L208. Test clock bypass → unreliable unit tests for reconciliation. | `/Receipts/Reverse` POST (workflow) | `ReceiptService.cs` ReverseReceiptAsync L208; compare other lines of same file using _dateTime.UtcNow correctly. | No | No | Financial accuracy of date fields (low probability of actual financial misstatement since it's real system clock; high probability of test defects hiding real bugs) | Yes | Yes | NO (testability issue not operational financial data loss) | Replace `DateTimeOffset.UtcNow` with `_dateTime.UtcNow` (the class already has the _dateTime field, confirm field exists then use it). | Unit test mock IDateTime return 2038 date. Reverse a receipt → Assert.ReversedAt == 2038 date. | OPEN |
| FAUD-0015 | MEDIUM | UserManagementController | AddUser creates Identity user then adds to role. No ambient transaction around CreateAsync(user, password) + AddToRoleAsync. If role-add fails (e.g., role deleted mid-process), orphaned user record exists in AspNetUsers with no role assignments = ghost user cannot login but occupies namespace + potential future security risk if role name re-added. | Static AddUser post action code review. | 1. POST UserManagement/AddUser with valid password valid role name. 2. Artificially fail AddToRoleAsync (e.g., rename role in DB transactionally between first and second call — simulate). | Both roll back together. No AspNetUsers row if role assignment fails. Role creation should either succeed atomically or no user row persisted. | AspNetUsers row COMMITTED. Role link NOT in AspNetUserRoles. User object exists with 0 roles. | `/UserManagement/AddUser` POST | `UserManagementController.cs` AddUser [HttpPost] action lines ~160-210. | DB — orphan user rows | Low — authorization not elevated (user has no roles) | No financial impact | Yes page lists orphan user with no role badge | No (mobile no users page) | **YES** — recoverable but ghost users bad security hygiene | Wrap both operations in TransactionScope or same EF transaction across UserManager. Use UserManager provided by Identity with same DbContext. Create transaction: `using var tx = _userManager.GetDb().Database.BeginTransaction(); create; addrole; tx.Commit;`. Check if identity supports this. Catch → tx.Rollback. | Test role name that doesn't exist → Assert 0 rows added to AspNetUsers. | OPEN |
| FAUD-0016 | MEDIUM | 6 Razor Views | Authorization via hardcoded role strings IsInRole("CompanyAdministrator") etc instead of using RoleNames static constants. 6 occurrences. If role renamed, views silently break (role checks return false even for valid user). Maintenance risk and consistency bug. | Static search of Views/*.cshtml for `IsInRole(` string literal. | 1. Developer wants to rename role CompanyAdministrator → OrgAdmin. 2. Updates RoleNames.cs constants. 3. Updates policies. 4. Forgets to update 6 views using hardcoded string literal. 5. Log in as SysAdmin expecting nav links. | All role checks uniformly use RoleNames.CompanyAdministrator constants; rename refactored automatically. | Navigation links / action buttons rendered with old role name. Even admin in new role cannot see actions. 6 specific UI places break silently. | N/A (view rendering only, 6 pages) | 6 cshtml files in Views folder using `IsInRole("...")` with string literal. | No | Medium risk: nav links hidden for users actually in correct role if constants change | No financial statement errors | Yes — Desktop nav only | No — mobile views likely unaffected | **YES** — UI degradation recoverable but should fix for consistency | Replace each IsInRole("LiteralRole") with IsInRole(RoleNames.LiteralRoleConstant). Add @using to import RoleNames class. Grep verify zero remaining string literal IsInRole across cshtml. | Smoke rename test: change RoleNames constant prefix → assert all views use constant recompile OK. | OPEN |
| FAUD-0017 | MEDIUM | Reports MobileSalesByPeriod | Terminology: "#Heads" / "# Heads" instead of "Number Sold". Placeholder FAUD-0001 example verified as genuine. 1 occurrence confirmed by grep. Reports.Mobile.SalesByPeriod line 42 card summary header. | Grep `# Head` pattern across cshtml. | Open Mobile /Reports/MobileSalesByPeriod. Inspect summary card header text. | Per spec: "Number Sold (Qty)" - consistent with desktop SalesByPeriod + spec wording explicitly Section 1. | Shows "# Heads: 27". Spec says terminology bug: "Number Sold" everywhere. | `/Reports/MobileSalesByPeriod` GET | `MobileSalesByPeriod.cshtml` L42. | No | No | No — cosmetic terminology only | No | Yes — mobile only page | NO (cosmetic terminology - Medium category description says documentation / terminology inconsistency OK with conditions) | Replace `# Heads` with `Number Sold` in card header. | Mobile 360×800 page renders new label "Number Sold". | OPEN |
| FAUD-0018 | MEDIUM | Payments/Create View (Desktop) | Allocation Preview table at line 99 <table class="table table-sm mb-0"> is NOT wrapped in `<div class="table-responsive">`. Every other table across the app is wrapped. This single missing wrapper causes page-level horizontal overflow on 1024×768 desktop viewport when table columns push past width. | Static analysis of table wrapping from search: "1 CONFIRMED CASE missing wrap Payments/Create L99". | 1. Navigate Payments/Create on 1024×768 desktop. 2. Populate invoice with many line items (allocation table grows horizontally >1024). 3. Check browser body.scrollWidth vs clientWidth. | Page-level horizontal overflow = 0. All table content scrolls within container. | Browser shows HORIZONTAL PAGE SCROLL BAR. Page itself scrolls horizontally; content clipped on left/right. Spec Section 14 explicitly forbids "No unnecessary page-level horizontal overflow". | `/Payments/Create` GET | `Views/Payments/Create.cshtml` L99 table; missing div.table-responsive parent. | No | No | No | Yes — 1024×768 viewport broken | No — payments no mobile page (mobile relies on desktop responsive alone) | **YES** — recoverable layout issue per medium spec responsive category. Wrap with <div class="table-responsive"> around that table. Align with pattern from Payments/Details which wraps correctly. | 1024×768 viewport: render Payments/Create; Assert document.body.scrollWidth == document.body.clientWidth (no horizontal overflow). | OPEN |
| FAUD-0019 | MEDIUM | restore-database.ps1 Script | Restore procedure does NOT call RESTORE VERIFYONLY before executing RESTORE DATABASE. Per DR best practices (and Section 19 spec: "SQL recognizes backup" → formal VERIFYONLY step missing). Invalid backup header goes straight to RESTORE which can partially initialize pages. | Static inspection lines of restore-database.ps1; grep for VERIFYONLY → zero hits. Section 19 mandates SQL recognizes backup. | 1. Take a valid .bak. 2. Truncate the last 512 bytes corrupting tail. 3. Run restore script. | Pre-flight RESTORE VERIFYONLY should fail → script exits non-zero with clear error. RESTORE DATABASE never attempted. | Script proceeds directly to RESTORE DATABASE against corrupted .bak. May fail mid-restore leaving target DB in RESTORING state, force manual SET MULTI_USER recovery path. Data recovery risk. | N/A (DR script) | scripts/restore-database.ps1 around line 80-106 before FILELISTONLY call. | Potential data loss if corrupted restore attempted against production with REPLACE flag. | No | No. | No. | No. | **YES** — if corrupted backup used for recovery in incident. Add call: `sqlcmd -S $Server -E -b -Q "RESTORE VERIFYONLY FROM DISK=N'$BackupFile' WITH STATS=5;"` If VERIFYONLY exit code ≠ 0 → write error log, exit code 19. | Test corrupt backup → script exits VERIFYONLY fail without touching target DB. Valid backup → VERIFYONLY success → RESTORE proceeds. | OPEN |
| FAUD-0020 | MEDIUM | SalesByPeriod Report | Report filters incomplete: Customer filter + Status selector UI not implemented. Spec Section 13 expects "Customer/type/status where available". Desktop SalesByPeriod.cshtml page has only From/To dates + Farm dropdown. Customer and Status filter controls missing from both UI and controller action binding. | Code inspection controller SalesByPeriod parameters + view filter bar. | Open Reports/SalesByPeriod. Look for Customer dropdown and Status dropdown (All/Confirmed/Completed per spec exclude Reversed/Cancelled already server-side). | Filter bar shows Date range, Farm, Customer (search-select), Status selector. Customer id bound to action parameter, passed into service filter. Status selector UI present. | Only Date range + Farm present. Customer cannot filter. Status = only hardcoded server Confirmed/Completed (good) but user cannot specify more granular. | `/Reports/SalesByPeriod` GET `/Reports/SalesByPeriod?format=csv` CSV `/Reports/MobileSalesByPeriod` GET Mobile | `ReportsController.cs` SalesByPeriod L70-103; `SalesByPeriod.cshtml` view; MobileSalesByPeriod.cshtml; `IReportService.cs` SalesByPeriod method signature; ReportService SalesByPeriod. | No | No | Slight — user has to export CSV then manually filter in Excel for customer → extra work. Minor risk if wrong customer totals selected manually in Excel. | Yes | Yes | NO (workaround exists) | Add customerId (Guid?) and status (string?) parameters to controller, service filter, view filter bar (both desktop SalesByPeriod and mobile using native mobile patterns). | Test 2 customers; filter by customerId=customerA → only customerA rows in detail + total. | OPEN |
| FAUD-0021 | MEDIUM | Architecture (Domain) | Layered architecture violation: Domain project carries `<PackageReference Include="Microsoft.EntityFrameworkCore">` production package reference. Domain should be persistence-ignorant ZERO dependencies on Infra-specific tech. Leaks EF specifics (attribute annotations, DbFunctions etc) can creep into Domain. Violates Section 4 audit dependency-direction rules. | csproj grep in Domain folder for EntityFramework. | Inspect `src/Domain/Domain.csproj` → PackageRef list. | Domain depends ONLY on language-level / std libs (Microsoft.Extensions.* maybe). Zero EntityFramework Core direct package reference in Domain. | `<PackageReference Include="Microsoft.EntityFrameworkCore" Version="X.Y.Z" />` DIRECTLY in Domain csproj. Persistence ignorance broken. | N/A (compile time) | `LivestockManager.Domain.csproj` file. | No. | Low (future coupling risk, not current vuln). | No. | Compile time risk, not runtime. | No. | NO — clean architecture violation not operational bug. | Move EF attribute usages (like [Timestamp] etc.) into EntityTypeConfigurations in Infrastructure (IEntityTypeConfiguration<T>). Remove EF ref from Domain.csproj. If Timestamp attribute needed anyway via System.ComponentModel.DataAnnotations, use that namespace which is in shared BCL, not EF. | Build after remove → 0 errors. NetArchTest LayerReferenceTests should still pass (this is a different rule but can add new rule: DomainAssembly ShouldNotHaveDependencyOn MicrosoftEntityFrameworkCore). | OPEN |
| FAUD-0022 | MEDIUM | AuditLog / AppDbContext | Audit logging gap: AuditLog entries' UserId and CompanyId columns always NULL across all entries. SaveChangesAsync override in AppDbContext calls CreateAuditLogsAsync but fails to populate / pass-through ICurrentUserService + company resolution. Spec Section 9 (Audit Logs) + Section 18 require correlation. | Static analysis of CreateAuditLogsAsync in AppDbContext. Inspect where UserId/CompanyId set. | 1. Perform any tracked change (e.g., create Livestock record). 2. Query AuditLogs table afterward. 3. Check UserId, CompanyId. | AuditLog.UserId = current logged-in user. AuditLog.CompanyId = user's company. | UserId = NULL. CompanyId = NULL for every row. Cannot trace "who changed what when for which company" = audit log useless for forensic/financial audit. | N/A (Db-level) | `AppDbContext.cs` SaveChangesAsync override; CreateAuditLogsAsync method body. Also `AuditLog.cs` entity fields. | **YES — important DB columns not populated** | **YES — audit log useless, violates security audit requirements** | Financial audit trail broken | Yes | Yes | **YES** | In AppDbContext, inject ICurrentUserService (already exists) into constructor. Before inserting AuditLog entries: set log.UserId = _currentUser.UserId ?? Guid.Empty; set log.CompanyId = resolvedCompanyId (from user or from entity.CompanyId fallback). Ensure at least one set correctly. | Test update entity via controller with known user → SELECT TOP 1 * FROM AuditLogs WHERE EntityId=x; Assert UserId=KnownUser; Assert CompanyId=KnownCompany. | OPEN |
| FAUD-0023 | MEDIUM | Startup + Infrastructure | 12 exception-swallow catch blocks (8 empty catch {}; 4 catch { return X } without logging). Highest risk Program.cs startup catches (FAUD-0006) are CRITICAL already registered. The remaining 10: DemoDataSeeder 3x AddToRole silent fail; DocumentNumberGenerator 2x prefix fallback; StockAdditionController 2x redirect without log; FormattedPdfWriter 1x font fail; Validator 1x OOXML fail (safe return false). | Pattern search `catch { }` and `catch { return` in src/. | Call site: DocumentNumberGenerator.InvoiceNumberPrefix async → SQL error → fallback INV prefix constant. If SQL connectivity degraded sequences might silently skip. | Every catch must at minimum log Log.Warning with exception details. Silent swallow hides root cause bugs. Financial exceptions should be rethrown after log (never swallowed). | Various: swallow redirect, swallow role add, swallow font embed. Each class of bug hides debugging info in production. | N/A (various code paths) | Program.cs; DemoDataSeeder.cs x3; DocumentNumberGenerator.cs x2; StockAdditionController.cs x2; FormattedPdfWriter.cs x1; ProtectedFileUploadValidator.cs x1 safe return-false acceptable. | Potential yes | Debug/production incident response difficulty | Financial debugging harder when errors swallowed | Yes | Yes | CONDITIONAL: 6 of the 12 are startup or financial-service related → YES release blocking for those 6. | For each: logger.LogWarning/Swallow pattern → add logging. Financial operations: after LogWarning → rethrow. Safe validator return-false is OK as-is. Add ILogger to controllers missing it. | Unit test inject failure for each. Check logs contain warning/error entry with correct stack trace. | OPEN |
| FAUD-0024 | LOW | Views (Percentages) | Ad-hoc percentage rendering via manual ToString and *100 math in 8 Razor locations instead of centralized PercentageDisplay helper. Dashboard Mortality% pre-multiplied by 100 in DTO so cannot call helper directly (need divide first). Reports ActiveLivestock PercentageOfTotal same. TaxRate ExpensesDetails = (fraction * 100) + " %". | Static search for literal `.ToString(` with % symbol in cshtml and * 100 math patterns. | Dashboard card shows mortality percentage. | Uses PercentageDisplay.Format helper like the Tax/Discount lines in Sales/Create. | 8 ad-hoc implementations: some use format 0.##, some 0.0#, different decimal places, slight inconsistency if value = 0.025 → some "2.5%" some "2.500%" some "#.## %" spacing. | Multiple pages where percentages render. | Views/Home/Index; Views/Reports/ActiveLivestock/MobileActiveLivestock; Views/Expenses/Details. Dashboard DTOs. | No. | No. | No. | Cosmetic. | Low (formatting inconsistency). | NO | Option A: Normalize all 8 behind PercentageDisplay helper. Add overload "PreMultiplied=true" for DTO-multiplied values. Option B: accept small inconsistency (cheaper). | Smoke test all pages render same value 0.025 → identical string "2.5%". | OPEN |
| FAUD-0025 | LOW | Configuration (Dead Key) | ConnectionStrings:FallbackConnection entry present in appsettings.json but NEVER READ by any code (grep "FallbackConnection" across C#). Dead configuration confuses deployment administrators. Can be accidentally populated with real values then ignored, causing debugging confusion "why doesn't this connection work?". | Static search "FallbackConnection" string C#; locations 0 usages found; only in appsettings.*json values (key) itself. | Inspect Program.cs + ServiceCollectionExtensions. | Remove dead key. | Legacy dead key present in 2 appsettings files. | N/A config-time. | appsettings.*json 2 files. | No. | Low confusion risk. | No. | No. | No. | NO | Delete ConnectionStrings:FallbackConnection from both appsettings.json and appsettings.Development.json. Document in CHANGELOG "Removed unused FallbackConnection key". | App boot + health + DB check still succeed after removal. | OPEN |
| FAUD-0026 | LOW | Mobile Coverage Gap | 12 of 20 Controllers have ZERO Mobile*.cshtml views: Account, Audit, Companies, Customers, Documents, Farms, Invoices, Payments, Receipts, Settings, Suppliers, UserManagement. Mobile users rely on desktop Bootstrap responsive alone for these pages. Section 15 audit spec expects mobile equivalents for all important pages; coverage gaps mean small-screen users must zoom. | Views enumeration: 75 desktop page views, 20 dedicated Mobile*.cshtml views for remaining 8 controllers. | Open mobile 360×800 viewport. Go to /Customers. Scroll horizontally. | Dedicated MobileCustomers.cshtml using card layout, 44px buttons, details panel, no horizontal overflow. | Renders desktop view. Zoom in-out needed. Tables may push past width without table-responsive wrapper (though most have it, see Payments/Create FAUD-0018). | Customers/Index; Companies/Index; Invoices/Index; Payments/Index; Receipts/Index; Settings/Index; Audit/Index; Suppliers/Index; Farms/Index; Documents/*; Account/Login; UserManagement/* | 12 Controllers × Views folders missing dedicated Mobile*. | No. | No. | No. | Not all desktop pages render perfectly (wrappers not 100%). | Yes mobile users on these pages get desktop experience only. | NO (per summary spec "Production configuration SAFE" not impacted; mobile coverage LOW priority given shared layout table-responsive class). | Defer to next round: priority order Customers/Invoices/Payments/Suppliers/Audit → create mobile equivalents. Deferred conditions acceptable per APPROVED WITH CONDITIONS if 0 CRITICAL HIGH remaining. | User Acceptance Test physical device: if Customers page renders without zoom on iPhone 15 OK, close. | OPEN (Low priority deferred) |
| FAUD-0027 | LOW | Sales/MobileCreate View | Line 390: bulk remove inline row button `style="min-height:36px;"`. 44px mobile touch target global standard. 36px slightly below 44px minimum. Very low impact: secondary action on row (remove line). | CSS style search min-height 36 across mobile views. | Mobile SalesCreate page, in-item remove row button (touch). | min-height:44px matching all other touch controls. | 36px. | /Sales/MobileCreate | Views/Sales/MobileCreate.cshtml L390. | No. | No. | No. | No. | Yes. | NO. | Change min-height: 36px → 44px. Align with rest of file's 23+ instances of 44px. | UI screenshot 44px height. | OPEN |
| FAUD-0028 | LOW | ProtectedFileUploadValidator + Storage | JPEG end-of-image trailer check (FF D9 magic bytes) reads the last 2 bytes correctly but compare result stored in a variable; the conditional block is empty → check never enforced. User can upload truncated JPEG without FF D9 terminator. Low impact since display is fine for most browsers. | ProtectedFileUploadValidator.cs lines 247-267 JPEG. | Upload a test JPEG file truncated before FF D9. | Validation should return failure ("JPEG image appears corrupted (missing end-of-image marker)."). | Bytes read, stored in local, variable never referenced; returns success = true. | /Documents/Upload POST | ProtectedFileUploadValidator.cs L257-266; mirror in ProtectedDocumentStorage VerifyMagicBytesEnhanced. | No DB. | Low file integrity. | No financial. | Desktop uploads OK. | Mobile uploads OK. | NO. | Add enforcement: if (!hasValidTrailer) return (false, "JPEG/JPG file missing valid end-of-image (FF D9) marker."). | Unit test truncated JPG bytes → validation fails. | OPEN |
| FAUD-0029 | LOW | ProtectedDocumentStorage | Internal filename generation uses `Guid.NewGuid().ToString("N") + ext` instead of `Path.GetRandomFileName()`. Both 32 hex chars = entropy identical (122 bits vs 128 bits), Guid slightly more predictable though still unguessable for practical document IDs. Not a real vulnerability. Spec Section 17 expects randomized internal names. | Code review line 110 in ProtectedDocumentStorage + validator output. | Inspect stored filename in Document StoragePath column. | Random 8.3 short name by Path.GetRandomFileName = opaque. Or Guid same. | StoragePath = "1e4c8e2088b744e2a0922afd4a05e09d.pdf" for example. Predictable? Not really. Guid V4 random is cryptographically random per spec. So same level of protection. | Documents/Upload → stored DB value. | ProtectedDocumentStorage.cs StoreAsync filename generation line. | No. | Low. | No. | No. | No. | NO. | Optional: keep as-is (guid random fine). Replace with Path.GetRandomFileName if internal team wants. | Smoke test upload → filename is random 32 chars. | OPEN (very low; keep as-is recommended). |
| FAUD-0030 | LOW | Reports (Scaffold placeholders) | 3 scaffold placeholder views unused: Reports/Create.cshtml, Reports/Details.cshtml, Reports/Edit.cshtml. Controller has no actions for Create/Details/Edit. Dead files. Reports route confusion for future developers. Same pattern for other controllers but checked: Invoices/Edit, Sales/Edit, Payments/Edit also have scaffold placeholder views with no matching action. | Static scan for views with no matching controller action. | Try browse /Reports/Create (no action → 404). | Dead placeholder views should be deleted or have corresponding actions implemented. | Views exist unused. | Reports/Create (404) Reports/Details (404) Reports/Edit (404). Also Invoices/Edit (404) Sales/Edit (404) Payments/Edit (404). | 6 dead scaffold views total. | No. | None. | None. | No functional. | No functional. | NO. | DELETE 6 views or implement missing actions. Delete is safer since confirmed immutability pattern means editing finalized invoices/sales is disallowed by design. | Build / routes / views 200 OK after deletion. | OPEN (cleanup) |

---

## Defect Counts (by Severity)

| Severity | Count |
|---|---|
| CRITICAL | **6** (FAUD-0001, FAUD-0002, FAUD-0003, FAUD-0004, FAUD-0005, FAUD-0006) |
| HIGH | **4** (FAUD-0007, FAUD-0008, FAUD-0009, FAUD-0010) |
| MEDIUM | **13** (FAUD-0011 through FAUD-0023 inclusive) |
| LOW | **7** (FAUD-0024 through FAUD-0030 inclusive) |
| Informational (non-defect notes) | **0** |
| **Release-Blocking Total (6 CRITICAL + 4 HIGH + 14 MEDIUM flagged = **24 BLOCKING**)** | **24 blocking defects must be remediated / conditioned before APPROVED or APPROVED WITH CONDITIONS** |

---

## Notes

- Any defect requiring schema change → automatically HIGH release-blocking unless emergency patch path documented + tested. Current defects NO schema changes (all code fixes).
- Any defect affecting tenant isolation Company A vs B → CRITICAL by definition. 4 CRITICAL defects are cross-tenant scope.
- Any defect that double-counts or omits profit line → FAUD-0004/0005/0007 (transactional inconsistency) CRITICAL/HIGH; FAUD-0012 formula wrong MEDIUM.
- Severity distribution: 6-4-13-7 typical profile for pre-release enterprise line-of-business app. Remediation priority: 1st → 6 CRITICAL (blocking, fix first). 2nd → 4 HIGH (blocking). Then 13 MEDIUM (10 blocking + 3 conditional OK). 7 LOW deferred.
