# THREAT MODEL
> **PHASE 10 CORRECTION NOTICE (2026-08-09):** This doc previously contained claims for features PROVEN NOT EXISTS in source code. Corrected items below are annotated **[NOT IMPLEMENTED. See docs/ROADMAP.md.]**. Authoritative existing features: docs/IMPLEMENTED_FEATURES.md.

## STRIDE Per-Module Analysis with Mitigations---## STRIDE Primer| Letter | Threat | Property Violated | Typical Example ||---|---|---|---|| **S** | **Spoofing** | Authenticity | Attacker pretends to be a legitimate user; stolen session cookie; fake login page. || **T** | **Tampering** | Integrity | Modifying data in transit or at rest; changing invoice total in request body; altering DB via SQLi. || **R** | **Repudiation** | Non-repudiation | User denies they performed an action with no evidence to prove otherwise. || **I** | **Information Disclosure** | Confidentiality | Leaking customer PII or company financial data via error messages, log files, direct object access. || **D** | **Denial of Service** | Availability | Brute-force login floods; request size bombs; slowloris; expensive report endpoints hammered. || **E** | **Elevation of Privilege** | Authorization | Viewer becomes Accounts; regular user becomes CompanyAdmin; cross-company write. |Rating scale used for Risk = Likelihood × Impact.Impact: Insignificant (1) → Minor (2) → Moderate (3) → Major (4) → Severe/Catastrophic (5).Likelihood: Rare (1) → Unlikely (2) → Possible (3) → Likely (4) → Almost Certain (5).Treat anything ≥ 12 as HIGH, ≥ 16 as CRITICAL.---## Module 1: Authentication & Identity| ID | STRIDE | Threat Description | L | I | R | Mitigations ||---|---|---|---|---|---|---|| AUTH-S-001 | **S**poofing | Credential stuffing / brute-force password attack against /Identity/Account/Login. | 4 | 4 | 16 ⚠ | **Lockout** (5 attempts → 5 min). **Rate limit** (10 req/min/IP). **Password length** 10+ chars, complexity. CAPTCHA on 3 failed (optional). **HIBP breach check** (optional). || AUTH-S-002 | **S**poofing | Stolen session cookie used from another machine (cookie theft via XSS or malware). | 2 | 5 | 10 | Cookie: `HttpOnly`, `Secure`, `SameSite=Lax`. Sliding expiry 30 min; absolute 12 h. **MFA (TOTP)** mandatory for SystemAdministrator; opt-in for other roles. || AUTH-S-003 | **S**poofing | Weak password reset token leaks (email interception; token too short). | 3 | 5 | 15 ⚠ | ASP.NET Core Identity UserManager `GeneratePasswordResetTokenAsync` uses DataProtected tokens with short TTL (default 1 day). Enforce HTTPS on reset page. Rate limit reset requests. Do NOT log reset tokens. || AUTH-T-004 | **T**ampering | JWT-like cookie claims altered on client to upgrade role/CompanyId. | 1 | 5 | 5 | Cookie is encrypted and signed by **DataProtection API** (not plain claims). DataProtection keys stored with ACL to AppPool only. DPAPI not used (roaming-safe). || AUTH-R-005 | **R**epudiation | User disclaims "I never logged in / changed password". | 2 | 3 | 6 | Audit log on every login (success/fail), password change, 2FA enroll, reset, role grant. Record `IpAddress`, `UserAgent`, timestamp UTC. Retain 7 years. || AUTH-I-006 | **I**nfo Disclosure | Verbose 500 error page leaks stack trace, connection strings, table names. | 3 | 3 | 9 | DeveloperExceptionPage only in Development; Production uses `/Error` generic page; Serilog captures details server-side only, never rendered. || AUTH-I-007 | **I**nfo Disclosure | Timing attack on login — `User not found` vs `Invalid password` — different response times leak which emails are registered. | 3 | 2 | 6 | Always return identical message: "Invalid login attempt." Run identical hashing work whether user exists (hash dummy) to equalize timing. || AUTH-D-008 | **D**oS | 50,000 login requests/min from botnet exhaust app pool CPU on expensive PBKDF2 hashing. | 3 | 4 | 12 ⚠ | Rate limit 10 req/min per IP + per user+IP. CDN/WAF rule (Azure Front Door / Cloudflare managed rule). Fail-fast lockout. || AUTH-E-009 | **E**levation | Broken role assignment page: POST /Users/Role with `Role=SystemAdministrator` works for CompanyAdmin (missing server-side policy). | 2 | 5 | 10 | All role-assignment endpoints require explicit `CanManageAnyUser` policy (SystemAdmin only for cross-company). `CanEditUserRoles` policy bounds max assignable rank ≤ `CompanyAdministrator` (rank 90), never `SystemAdministrator`. Integration test INT-level + architecture rule (ARCH): role rank bound. || AUTH-E-010 | **E**levation | Dev seed user `admin@example.com / Passw0rd!` accidentally exists in Production. | 2 | 5 | 10 | **Dual guard**: `ASPNETCORE_ENVIRONMENT == Development` **AND** `ENV_ENABLE_DEV_SEED == true`. Additional runtime insert guard rejects emails ending `@example.com` in Production. Integration test `TST-INT-011` gates CI/deploy. |---## Module 2: Company Isolation / Multi-Tenancy| ID | STRIDE | Threat Description | L | I | R | Mitigations ||---|---|---|---|---|---|---|| ISO-S-011 | **S**poofing | User of CompanyA crafts request URL `/Invoices/Edit/{id_from_CompanyB}` with GUID brute force. | 4 | 5 | 20 🔴 | **3-layer isolation**: (1) Global EF Core CompanyId query filter (automatic WHERE). (2) SaveChangesAsync company guard on writes. (3) `ManageInCompanyRequirement` authorization handler checks `User.CompanyId == Route.CompanyId` BEFORE handler runs. Integration test `INT-001` — cross-read returns zero rows. || ISO-T-012 | **T**ampering | User submits POST /Livestock/edit with hidden form field `CompanyId = OtherCompany`. | 3 | 5 | 15 ⚠ | CompanyId in entity **never bound from user input** (BindNever on DTOs; set inside handler from `ICompanyContext`). Architecture test ARCH-010 enforces policy, not roles. EF guard rejects mismatch. || ISO-I-013 | **I**nfo Disclosure | Global search endpoint (REQ-UI-009) leaks cross-company invoices when searching invoice number text. | 2 | 5 | 10 | Search query explicitly adds `.Where(e => e.CompanyId == CurrentCompanyId)` before `.Where(freeText)`. Integration test: 2 companies, same invoice numbers, search returns 1 result. || ISO-E-014 | **E**levation | SystemAdministrator company switcher (impersonation) leaves no audit trail → SysAdmin performs fraud in Company A, denies. | 2 | 5 | 10 | Impersonation session claim `ImpersonatingCompanyId` logged on EVERY audit log during impersonation; UI banner reminds admin. Separate sysadmin-only audit search filter "Actions while impersonating". |---## Module 3: Livestock Registry| ID | STRIDE | Threat Description | L | I | R | Mitigations ||---|---|---|---|---|---|---|| LV-I-015 | **I**nfo Disclosure | Farmer (FarmManager restricted to FarmA) enumerates livestock in FarmB of same company via sequential farm id in URL. | 3 | 2 | 6 | Farm-scoped authorization policy `CanManageFarmFarmId`. Livestock list filter forced: `userManager.GetAssignedFarmsFor(user)` filter. Viewer sees only livestock of farms they are assigned. || LV-T-016 | **T**ampering | Backdated weight entries entered fraudulently to inflate valuation (post-sale modification). | 2 | 4 | 8 | Weight entries immutable after write (attempt UPDATE trigger in DB → RAISEERROR; validator rejects on write). Corrections only via ADD + soft-delete old, with Reason field. || LV-D-017 | **D**oS | CSV import of 1M rows causes CPU exhaustion and long-running transaction. | 3 | 3 | 9 | Hard cap 10 000 rows per CSV. 10 MB file cap. Cancellation token on parsing. Bulk insert via SqlBulkCopy with timeout 5 min, chunked 1000 rows per commit. Rate limit 1 import per minute per company. || LV-R-018 | **R**epudiation | Livestock marked "Dead" by malicious insider, then insider claims the system auto-deleted it. | 2 | 3 | 6 | Audit log captures every Status change with `OldValues` → `NewValues` JSON. Status change is irreversible (cannot go back to Alive except on Invoice void, which itself is separately audited). |---## Module 4: Purchases & Expenses| ID | STRIDE | Threat Description | L | I | R | Mitigations ||---|---|---|---|---|---|---|| PE-T-019 | **T**ampering | Accounts user posts purchase → after posting, changes `UnitPrice` directly via tampered POST (bypasses UI readonly on posted docs). | 3 | 5 | 15 ⚠ | Purchases marked `Status = Posted` throw `InvalidStatusTransitionException` on any attempted mutation. EF validator rejects change via `Entry.State` check. || PE-T-020 | **T**ampering | Purchase line item UnitPrice modified via JSON injection on AJAX add-line. | 3 | 3 | 9 | FluentValidation validates range `UnitPrice >= 0 && < 1 000 000`; total cross-check: `Sum(lines) ≈ header totals, else ValidationException`. || PE-I-021 | **I**nfo Disclosure | Attachments storage path traversal — `GET /Files/Download?storageKey=../../../web.config` serves `web.config` with secrets. | 2 | 5 | 10 | StorageKey is **GUID filename, not path**. Path navigation check: `Path.GetFileName(storageKey) != storageKey` → 400. File content streamed via FileContentResult with `Content-Disposition: attachment`. Files never stored under wwwroot; served ONLY via controller that checks user permission against company. || PE-R-022 | **R**epudiation | Employee buys personal items, submits fake expense. Later claims: "I never uploaded this receipt." | 2 | 3 | 6 | StoredFile row records `CreatedById`, original filename, SHA-256 of bytes, IP address. Expense row FK → StoredFileId (cascade delete blocked — expenses reference attachments, attachments cannot be deleted until expense is deleted). || PE-D-023 | **D**oS | Upload zip-bomb / decompression bomb disguised as a `.xlsx` (Office zip file, inside XML 10 GB). Validator reads first bytes to verify, loads entire into memory → OOM. | 3 | 3 | 9 | 10 MB cap. Do NOT open XML/Office content server-side for non-report purposes; store-and-forward file storage only; do not parse. Excel export done app-to-file, not parsing user upload. Magic-byte + extension check only. |---## Module 5: Sales & Invoicing (Highest-Risk Module)| ID | STRIDE | Threat Description | L | I | R | Mitigations ||---|---|---|---|---|---|---|| SI-T-024 | **T**ampering | DataEntry edits Invoice `GrandTotal` on an already-Sent invoice to reduce what customer owes, in collusion with the customer. | 3 | 5 | 15 ⚠ | Status = Sent is IMMUTABLE (except for `LastSentAt` counters and PaidAt/AmountPaid from payments). Any attempt to modify line/price triggers `InvalidStatusTransitionException`. Only `Void` can cancel, and Void requires Accounts+ role with mandatory reason string ≥ 10 chars, plus audit log entry of full before/after. || SI-T-025 | **T**ampering | Client-side JavaScript total calculation bypassed — POST with GrandTotal = 0.00 while DB rows are normal, to defraud company. | 3 | 5 | 15 ⚠ | Server ALWAYS recalculates totals from lines server-side before save. Client totals are purely for display convenience. UnitOfWork behavior runs validation: `header.GrandTotal == Recalc(lines)` or auto-correct & log discrepancy (auto-correct preferred to avoid drift). || SI-T-026 | **T**ampering | SQL injection on invoice list search field `?keyword='; DROP TABLE Invoices;--`. | 2 | 5 | 10 | All queries EF LINQ or `FromSqlInterpolated` with interpolated params; raw concat → arch test + code review SAST. || SI-S-027 | **S**poofing | Phishing to user → malware → session cookie hijacked → attacker issues invoice to fake customer with their own bank details in the footer. | 2 | 5 | 10 | Bank info editable only by CompanyAdministrator (policy `CanEditOwnCompany`). Every change to bank info is emailed as notification to all CompanyAdministrators of that company (additional defense-in-depth notification). || SI-I-028 | **I**nfo Disclosure | PDF Invoice endpoint `/Invoices/{id}/pdf` accessible without auth via direct URL guess (GUID enumeration). | 2 | 5 | 10 | Action requires `CanDownloadInvoicePdf` policy (all authenticated roles OK BUT company guard → rejects cross-company). PDF bytes streamed only after verifying `Invoice.CompanyId == CurrentUser.CompanyId`. || SI-R-029 | **R**epudiation | Customer claims "invoice was never sent". Email send failure logged but no proof of delivery. | 3 | 3 | 9 | Log `SendGrid message id` (`ProviderMessageId`) in OutboundEmails table; request delivery receipts + open tracking optional. LastSendStatus=Sent with timestamp provides audit. || SI-D-030 | **D**oS | 1000 PDF-generation requests/min for big invoices with long line tables exhausts CPU/RAM. | 3 | 3 | 9 | Hangfire queue with concurrency limit (default N=2 PDF workers). Rate limit: 20 invoices/min/user. Cache generated PDF blobs in StoredFiles (idempotent). Serilog performance warnings > 5 seconds. || SI-E-031 | **E**levation | DataEntry user calls POST /Invoices/id/Void (action requires Accounts+ but endpoint missing `[Authorize(Policy=CanVoidInvoice)]` — regression). | 3 | 5 | 15 ⚠ | Void endpoints use explicit policy; architecture rule ARCH scans; integration test; policy-based void button hidden in UI (UI guard NOT a substitute — endpoint must enforce). || SI-E-032 | **E**levation | Overdue invoice status secretly set to paid by editing AmountPaid on invoice directly via PUT with arbitrary JSON — bypasses Payment allocations, no audit trail. | 2 | 5 | 10 | `AmountPaid` and `BalanceDue` on Invoice are DENY direct edit by DTO (no setter in public, or BindNever). Only `PaymentAllocation` handler writes via the allocation service. |---## Module 6: Payments & Receipts| ID | STRIDE | Threat Description | L | I | R | Mitigations ||---|---|---|---|---|---|---|| PR-T-033 | **T**ampering | Bookkeeper records a $10 000 customer payment with $9 900 allocated to oldest invoice and $100 to friend's fake customer to pocket $100 by under-reporting (fractional siphon). | 2 | 4 | 8 | Payment.TotalAllocated + Unallocated must equal Payment.Amount. Overpayment creates CREDIT NOTE row, not lost. Cross-footing validation sum of allocations ≤ Amount; strict equality. || PR-T-034 | **T**ampering | Void a payment (roll back) without leaving a traceable reason. | 2 | 5 | 10 | Payment void requires `VoidReason` mandatory ≥ 10 chars; IsVoided column + VoidedById + VoidedAt; allocations `IsReversed` column; all logged to AuditLog. || PR-R-035 | **R**epudiation | Company receives check but Accounts user claims "never received" and keeps check. | 1 | 3 | 3 | Dual-authorization optional (future feature); deposit slip scanned and attached to Payment as image StoredFile. |---## Module 7: Reports & Data Export| ID | STRIDE | Threat Description | L | I | R | Mitigations ||---|---|---|---|---|---|---|| RP-I-036 | **I**nfo Disclosure | Viewer role (no-export) exports P&L by modifying role claim in cookie (tampered cookie AUTH-E-004 already mitigated; but direct API call to export with Viewer cookie by tampering only role claim). | 2 | 4 | 8 | Export endpoints require `CanExportReports` policy which requires DataEntry+ rank. Viewer = rank 10; policy check before controller code executes. 403 Forbid on fail. || RP-D-037 | **D**oS | Report with 3-year date range, no filters → 1 000 000 rows pulled into memory for Excel export, hangs SQL and worker process. | 3 | 4 | 12 ⚠ | Report queries have hard MAX period limit (default 366 days; SystemAdministrator can bypass). Export row cap 100 000 rows; beyond, ask user to reduce range. Server-side pagination on report grid; streaming CSV/Excel (never materialize full list). SQL MAXDOP hint via EF. || RP-I-038 | **I**nfo Disclosure | CSV export downloaded — filename reflected in `Content-Disposition` header reflected as `" ><script>alert(1)</script>.csv" — content-sniff based XSS. | 2 | 3 | 6 | Strict allow-list only alphanumeric, underscore, hyphens, dot in export filename; strip other chars. `X-Content-Type-Options: nosniff` header. CSV content: prefix first line with `"\ufeff"` (UTF-8 BOM); field quoting per RFC4180. |---## Module 8: File Uploads / Attachments| ID | STRIDE | Threat Description | L | I | R | Mitigations ||---|---|---|---|---|---|---|| UP-T-039 | **T**ampering | Polyglot file attack: file `invoice.pdf.exe`, or `shell.php` renamed to `.pdf`, uploaded — then executed by navigating directly. | 4 | 5 | 20 🔴 | Double-layer validation: (1) extension allow-list `pdf/jpg/jpeg/png/doc/docx/xls/xlsx/csv` case-insensitive. (2) **Magic-byte signature check** on first N bytes (JPEG FFD8FF, PNG 89504E47, PDF %PDF; Office = ZIP + ext, CSV = no NUL bytes). Server renames to GUID — never keeps original name. Files NOT under wwwroot — delivered ONLY via auth controller with `Content-Disposition: attachment`; never `inline` (prevents MIME-sniff PDF/image polyglots from executing in browser context). || UP-I-040 | **I**nfo Disclosure | Attachments cross-company leak: `GET /Files/{otherCompanyId}/{storedFileId}` works with GUID enumeration. | 2 | 5 | 10 | Lookup StoredFile.CompanyId vs CurrentUser.CompanyId in download controller BEFORE open stream. 404 if not found (no 403 to avoid existence enumeration; GUID entropy is secure). || UP-D-041 | **D**oS | 1 000 × 10 MB uploads (10 GB) from one company in 1 hour → disk full. | 3 | 3 | 9 | Per-company storage quota (default 50 GB, warning at 80%, hard deny at 100%). 10 MB individual file limit. WAF / IIS request filter `maxAllowedContentLength=11534336` double-enforced. || UP-I-042 | **I**nfo Disclosure | Malicious file stored as CSV with `=cmd|' /C calc'!A0` cell formula executed by victim opening CSV in Excel (CSV injection attack). | 3 | 2 | 6 | CSV export from UI prefixes any cell that starts with `=`, `+`, `-`, `@`, `|`, `%` with single quote `'` to neutralize formula trigger. Documentation warns users. |---## Module 9: System & Infrastructure| ID | STRIDE | Threat Description | L | I | R | Mitigations ||---|---|---|---|---|---|---|| INF-S-043 | **S**poofing | Attacker brute-forces Hangfire dashboard `/hangfire` basic auth default. | 2 | 5 | 10 | Hangfire dashboard requires SystemAdministrator role; no basic auth. URL `/hangfire` NOT linked anywhere. Dashboard filter `HangfireSystemAdminAuthorizationFilter` rejects anything else. Dashboard bound only to HTTPS. || INF-I-044 | **I**nfo Disclosure | `appsettings.Production.json` accidentally committed to git repo → connection strings with passwords public. | 3 | 5 | 15 ⚠ | **No secrets in appsettings**. `appsettings.Production.json` in `.gitignore`. CI step: GitLeaks scan every commit; block PR if `Password=` or API key regex found anywhere in diff. Connection strings only via env vars or Azure KeyVault. || INF-T-045 | **T**ampering | DB connection string password captured from IIS `applicationHost.config` readable by low-privilege user on web server. | 2 | 5 | 10 | Prefer **Trusted Connection** (AppPoolIdentity Windows Auth to SQL Server; NO password in connection string at all). SQL firewall allow-lists ONLY web server outbound IP. SQL logins with strong passwords stored in Azure KeyVault or env ACL'd to AppPool only. || INF-D-046 | **D**oS | Application-wide — 10k RPS Layer-7 floods from botnet. | 3 | 4 | 12 ⚠ | CDN/WAF in front (Azure Front Door Premium, Cloudflare Pro). Managed rule sets: OWASP Core Rule Set 3.x + Bot mitigation. Rate limit 500 req/min/user in-app. Health probe in CDN to pull bad node out. AppPool CPU > 90% auto-scale out. || INF-E-047 | **E**levation | Weak IIS ACLs: AppPoolIdentity has write to `wwwroot` → attacker uploads webshell via file upload folder. | 2 | 5 | 10 | ACL `wwwroot` RX only (Modify only on `logs/`, `App_Data/`, `files/` outside `wwwroot`). File uploads never land in wwwroot. URL Rewrite rule blocks `/files/` path. || INF-E-048 | **E**levation | Dependency chain: malicious NuGet package compromised (supply chain). | 2 | 4 | 8 | `Directory.Packages.props` Central Package Management with pinned versions + Package Source Mapping. Dependabot automated. `dotnet list package --vulnerable` CI gate; NuGet audit. Critical packages (QuestPDF, EF Core) locked to signed Microsoft / verified maintainers. |---## Module 10: Admin & Audit Log| ID | STRIDE | Threat Description | L | I | R | Mitigations ||---|---|---|---|---|---|---|| ADM-R-049 | **R**epudiation | Insider sysadmin creates fraud invoice then clears AuditLogs with DELETE to cover tracks. | 2 | 5 | 10 | AuditLogs table `DENY DELETE` permission to AppPool SQL user; only SQL agent cleanup job (7-year archive) can delete via a stored procedure signed with certificate. App-level code NEVER deletes audit rows. || ADM-I-050 | **I**nfo Disclosure | Audit Log page shows user passwords in `NewValues` JSON after password change (old plaintext captured accidentally). | 2 | 4 | 8 | Audit interceptor explicitly excludes from logging: `PasswordHash`, `SecurityStamp`, `PasswordChangedAt` (value only, no hash), 2FA TOTP secret. Whitelist approach for logged properties, not blacklist. || ADM-E-051 | **E**levation | User role escalated by another CompanyAdministrator for cash bribe. No second approver. | 2 | 5 | 10 | All role-assignment events (UserRoleAssignmentAudits table) + email alert to every SystemAdministrator of company when role rank ≥ 50 changes. Optional future 4-eye approval workflow. |---## Overall Risk SummaryTotal threats: **51**.| Risk Band | Count | IDs (representative) ||---|---|---|| 🔴 CRITICAL 20+ | 2 | ISO-S-011, UP-T-039 || ⚠ HIGH 12–19 | 9 | AUTH-S-001, AUTH-D-008, PE-T-019, SI-T-024/25/31, RP-D-037, INF-I-044, INF-D-046 || MEDIUM 6–11 | ~28 | e.g. AUTH-I-006, LV-I-015, SI-R-029, UP-I-040, ADM-I-050 || LOW <6 | ~12 | |### All CRITICAL and HIGH threats must have mitigations IMPLEMENTED and passing integration test on merge to `main`. No go-live until 51 mitigations verified per § below.---## Mitigation Verification Matrix (Quality Gate)Threat ID → Automated Test → Manual Pen-Test / Review| ID | Automated Check | Manual Check | Status placeholder ||---|---|---|---|| ISO-S-011 | INT-001 cross-company read 0 rows; 3-layer guard unit tests | ZAP authenticated scan brute-force GUIDs | ☐ || UP-T-039 | INT-008 allow-list; INT-009 exe→pdf magic reject; arch rule extension | Manual: upload `calc.exe` renamed `calc.pdf` → 400; upload real pdf → 201 | ☐ || AUTH-S-001 | Rate limit integration test; lockout test INT-004 | Hydra 50k passwords against test instance | ☐ || AUTH-D-008 | k6 load test flood; rate limit kicks in; P95 < 500 ms | WAF/CDN attack simulation | ☐ || PE-T-019 | Unit test: post purchase then modify UnitPrice → exception | Manual POST tampering with Burp Suite | ☐ || SI-T-024 | Unit: edit Sent invoice → InvalidStatusTransitionException | Burp tamper: POST modify GrandTotal field on sent invoice → rejected | ☐ || SI-T-025 | Unit: totals auto-recalc; mismatched header total; assertion fails if server doesn't overwrite | Burp tamper total = 0, verify recalculated saved = proper amount | ☐ || SI-E-031 | Integration test: DataEntry role POST void → 403 | Manual: as DataEntry user, try void page access | ☐ || RP-D-037 | Integration: 2-year range → validated rejected; 367-day range → 400 | Manual long range, monitor SQL time with Profiler | ☐ || INF-I-044 | CI gitleaks step + dotnet list package --vulnerable | Security review: `appsettings*.json` contain any `Password=`? | ☐ || INF-D-046 | k6 scenario 1k rps, no 5xx >1% | Load test + WAF review before go-live | ☐ |All ☐ must be ✓ before production deployment.---## Revision History| Date | Version | Changes ||---|---|---|| 2026-08-06 | 1.0 | Initial threat model baseline (51 STRIDE threats). |