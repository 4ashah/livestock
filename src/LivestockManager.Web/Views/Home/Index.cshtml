@using LivestockManager.Application.DTOs.Livestock
@using LivestockManager.Application.DTOs.Invoices
@using LivestockManager.Domain.Enums
@{
    ViewData["Title"] = "Dashboard";
    var activeCount = ViewData["ActiveLivestockCount"] as int? ?? 0;
    var dischargedCount = ViewData["DischargedTodayCount"] as int? ?? 0;
    var revenue = ViewData["RevenueMonthToDate"] as decimal? ?? 0;
    var profit = ViewData["ProfitMonthToDate"] as decimal? ?? 0;
    var recentLivestock = ViewData["RecentLivestock"] as IEnumerable<LivestockSummaryDto>;
    var recentInvoices = ViewData["RecentInvoices"] as IEnumerable<InvoiceSummaryDto>;
}

<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pb-2 mb-4">
    <div>
        <h1 class="h3 mb-1">📊 Dashboard</h1>
        <div class="text-muted small">Today is @DateTime.Today.ToString("dddd, MMMM d, yyyy")</div>
    </div>
</div>

<div class="row g-3 mb-4">
    <div class="col-md-6 col-lg-3">
        <div class="card kpi-card h-100 border-0" style="border-left-color: #2d6a4f;">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="rounded-circle p-3 me-3" style="background-color: rgba(45,106,79,0.1);">
                        <span class="fs-4" style="color: #2d6a4f;">🐑</span>
                    </div>
                    <div>
                        <h6 class="text-muted mb-1 small text-uppercase">Active Livestock</h6>
                        <h3 class="mb-0 fw-bold text-dark">@activeCount.ToString("N0")</h3>
                    </div>
                </div>
                <div class="mt-2 small text-muted">Total currently in inventory</div>
            </div>
        </div>
    </div>
    <div class="col-md-6 col-lg-3">
        <div class="card kpi-card h-100 border-0" style="border-left-color: #e76f51;">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="rounded-circle p-3 me-3" style="background-color: rgba(231,111,81,0.1);">
                        <span class="fs-4" style="color: #e76f51;">🚚</span>
                    </div>
                    <div>
                        <h6 class="text-muted mb-1 small text-uppercase">Discharged Today</h6>
                        <h3 class="mb-0 fw-bold text-dark">@dischargedCount.ToString("N0")</h3>
                    </div>
                </div>
                <div class="mt-2 small text-muted">Sales / mortality today (UTC)</div>
            </div>
        </div>
    </div>
    <div class="col-md-6 col-lg-3">
        <div class="card kpi-card h-100 border-0" style="border-left-color: #2a9d8f;">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="rounded-circle p-3 me-3" style="background-color: rgba(42,157,143,0.1);">
                        <span class="fs-4" style="color: #2a9d8f;">💰</span>
                    </div>
                    <div>
                        <h6 class="text-muted mb-1 small text-uppercase">Revenue MTD</h6>
                        <h3 class="mb-0 fw-bold text-dark">$@revenue.ToString("N2")</h3>
                    </div>
                </div>
                <div class="mt-2 small text-muted">Confirmed invoices this month</div>
            </div>
        </div>
    </div>
    <div class="col-md-6 col-lg-3">
        <div class="card kpi-card h-100 border-0" style="border-left-color: #52b788;">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="rounded-circle p-3 me-3" style="background-color: rgba(82,183,136,0.1);">
                        <span class="fs-4" style="color: #52b788;">📈</span>
                    </div>
                    <div>
                        <h6 class="text-muted mb-1 small text-uppercase">Profit MTD</h6>
                        <h3 class="mb-0 fw-bold @(profit >= 0 ? "text-dark" : "text-danger")">@(profit >= 0 ? "$" : "-$")@Math.Abs(profit).ToString("N2")</h3>
                    </div>
                </div>
                <div class="mt-2 small text-muted">Gross profit on livestock sold</div>
            </div>
        </div>
    </div>
</div>

<div class="row g-3">
    <div class="col-lg-6">
        <div class="card shadow-sm border-0 h-100">
            <div class="card-header bg-transparent border-bottom d-flex justify-content-between align-items-center">
                <h6 class="mb-0 fw-bold">🐑 Recent Livestock</h6>
                <a asp-controller="Livestock" asp-action="Index" class="small">View all →</a>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-sm table-hover mb-0 align-middle">
                        <thead class="table-light">
                            <tr>
                                <th>ID</th>
                                <th>Type</th>
                                <th>Status</th>
                                <th>Weight</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (recentLivestock != null && recentLivestock.Any())
                            {
                                @foreach (var l in recentLivestock)
                                {
                                    <tr>
                                        <td><code>@l.LivestockId</code></td>
                                        <td>@l.LivestockTypeId</td>
                                        <td>
                                            @if (l.Status == LivestockStatus.Active)
                                            { <span class="badge bg-success">Active</span> }
                                            else
                                            { <span class="badge bg-secondary">@l.Status</span> }
                                        </td>
                                        <td>
                                            @if (l.CurrentWeight.HasValue)
                                            { @l.CurrentWeight.Value.ToString("F2") @l.WeightUnit }
                                            else
                                            { <span class="text-muted small">—</span> }
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr><td colspan="4" class="text-center py-3 text-muted small">No recent livestock.</td></tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    <div class="col-lg-6">
        <div class="card shadow-sm border-0 h-100">
            <div class="card-header bg-transparent border-bottom d-flex justify-content-between align-items-center">
                <h6 class="mb-0 fw-bold">📄 Recent Invoices</h6>
                <a asp-controller="Invoices" asp-action="Index" class="small">View all →</a>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-sm table-hover mb-0 align-middle">
                        <thead class="table-light">
                            <tr>
                                <th>#</th>
                                <th>Date</th>
                                <th class="text-end">Total</th>
                                <th>Status</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (recentInvoices != null && recentInvoices.Any())
                            {
                                @foreach (var i in recentInvoices)
                                {
                                    <tr>
                                        <td><code>@i.InvoiceNumber</code></td>
                                        <td>@i.InvoiceDate.ToString("yyyy-MM-dd")</td>
                                        <td class="text-end fw-semibold">$@i.GrandTotal.ToString("F2")</td>
                                        <td>
                                            @if (i.Status == InvoiceStatus.Paid)
                                            { <span class="badge bg-success">Paid</span> }
                                            else if (i.Status == InvoiceStatus.PartiallyPaid)
                                            { <span class="badge bg-primary">Partial</span> }
                                            else if (i.Status == InvoiceStatus.Draft)
                                            { <span class="badge bg-light text-dark border">Draft</span> }
                                            else if (i.Status == InvoiceStatus.Overdue)
                                            { <span class="badge bg-danger">Overdue</span> }
                                            else if (i.Status == InvoiceStatus.Confirmed)
                                            { <span class="badge bg-info">Confirmed</span> }
                                            else
                                            { <span class="badge bg-secondary">@i.Status</span> }
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr><td colspan="4" class="text-center py-3 text-muted small">No recent invoices.</td></tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
